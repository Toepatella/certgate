# CLAUDE.md — CertGate project guide

Certified selective prediction with explainable abstention for multi-site clinical risk models. Standalone Python research project targeting a paper. Read this first, then `SPEC.md` before touching any code.

## What this is

A selective-prediction gate that certifies, with finite-sample confidence 1−δ, that the error rate among *answered* cases stays ≤ α — treating the **site (cluster), not the record**, as the unit of statistical independence. Two assumption modes (exchangeable baseline; BBSE label-shift with cluster-robust uncertainty), an explainable-abstention layer, and an honest validation harness (verified-falsifiable negative controls; a two-number violation protocol).

**Target venue:** Discover Computing (Springer) — collection *"Intelligent Medicine: ML and Explainable AI for Next-Generation Healthcare"*, **submission deadline 2026-10-05**. See `PAPER-OUTLINE.md`.

**Provenance:** this is a deliberately simplified v2 restart of an audited v1 testbed. Every scope cut (covariate-shift mode, kNN screen, temporal machinery, preregistration apparatus) and every hardening (loud input validation, sha256-only seeds, record-carrying cluster gate, bootstrap top-up-or-decline, provenance blocks, pinned deps, literal-pinned constants test) traces to a specific v1 audit finding. The audit lessons are native to this design — **do not regress them.**

## Status (2026-07-25)

Built, tested, and validated end-to-end on synthetic data. Suite **190 passed / 3 skipped green** (~30s; three off-default arms gated behind `CERTGATE_FIXTURE=1` / `CERTGATE_EICU=1` / `CERTGATE_EICU_LARGE=1`). The **eICU-CRD v2.0 real-data path** landed 2026-07-30 against a protocol frozen *before* any eICU byte was read (`EICU-PROTOCOL.md`, SPEC §"Real-data protocol"); it runs end to end today on a schema-faithful mock corpus and awaits the credentialed extract — see the eICU section below. **A 2026-07-31 three-verifier ingest audit found one CRITICAL and six major defects in that path; all are fixed** (SPEC first, then code, then tests) — see the eICU section. **A same-day three-agent arrival-day audit (attack corpora, not code reading) found one further CRITICAL and one major, both fixed as E-21/E-22 (amendment A5, threats T-26/T-27):** an absent or UNLINKED APACHE block — float-formatted join keys, a header-only child table, or a key shift with row counts intact — certified silently with 89/161 constant columns and E-9's gate unevaluable (`gate_applies=false` at zero coverage); now `unparseable-join-key` + `apache-coverage-collapse` abort in `build_raw` and preflight projects both. The `unrecognised-null-token` gate was APACHE-only; it now covers the patient numerics too (whose `hospitaladmitoffset` doubles as the first-stay tie-breaker, so its silent loss also reordered cohort selection). The 2026-07-25 correctness audit (`CODE-AUDIT.md`, V1–V27) confirmed the betting core sound under six independent attacks and found the damage in the **claims around it**; every finding is fixed (SPEC first, then code), a second adversarial verification pass then broke and re-fixed the fixes themselves (21 demonstrated findings, including a false BBSE-row estimand clause, a tiny-K q-bootstrap hole closed by `BBSE_MIN_TARGET_SITES`, and a data-seeded BBSE bootstrap replacing label-seeding), and 22/22 verified mutations now go red against the hardened suite (the original 14 were 0/14). The two criticals: (V1) the certificate claimed a per-target-site bound where the math proves a **site-population average** — the guarantee text, SPEC, METHODS, and E1's conformance metric now state the estimand honestly, with a mandatory dispersion clause; (V2) BBSE treated the target predicted-positive rate `q_t` as exact — it now carries its own confidence share (Clopper–Pearson / cluster bootstrap, `BBSE_BONFERRONI = 4`, 16-corner box). Earlier passes: internal red-team 2026-07-22 (`REDTEAM.md`, R1 fixed); the 2026-07-25 editorial panel on the *manuscript* returned major revision (`paper/review/`). Headline results (full grid R=200, rerun 2026-07-25, `experiments/out/summary.md`):

- **E1 validity (rescored to the certified estimand):** α=0.10 certifies every draw at 0.98 coverage; aggregate R_M-exceed rate **0.0 ≤ δ=0.05** on fresh 200-site pools, holding at s_u ∈ {0.5, 1.0, 2.0} while the **per-site dispersion diagnostic** rises 0.02 → 0.10 (aggregate-vs-individual signature, measured and labeled). α=0.05 not reachable at 208 sites.
- **E2 label shift (0.095→0.22, documented sep=2.2):** uncorrected baseline hard-violates **39.5%** — and violates the aggregate estimand itself on **97.5%** of certificates (rescored 2026-07-30); BBSE with the honest q_t budget **declines 200/200** (194 failsafe / 6 misspecified) — zero certificates, zero certify-and-violate. Magnitude sweep (0.095/0.13/0.16/0.19/0.22): baseline failure dose-responsive (hard 0→0.395, aggregate 0→0.975), BBSE certify-and-violate 0.0 everywhere, and at null shift BBSE certifies 9% — a correction, not a reflexive decliner.
- **E3 concept control:** tilt verified to push true risk to 0.161 > α, then the certificate hard-violates 70.0% and violates the aggregate estimand on **100%** of certificates — the assumption tag is load-bearing.
- **E4 frontier:** α=0.10 certifies from ~150 sites up; α=0.05 first appears ~300 (0.285), reliable by 400. At the real 208-site scale α=0.10 is the operative rung.
- **E5/E6:** exact interventional-Shapley linear attributions + abstention explanations; per-site coverage 0.98–0.99 with answered error under α; three-way answered-set composition (predicted 6.3% / BBSE-implied 8.6% / oracle 9.2%). E5's R=200 replication arm (2026-07-30) returns the honest NULL: no stable abstention driver (top-feature share 0.274 ≈ 0.25 chance over 2,644 pooled declines) — the old n=2 "feature 0 driver" reading is formally retired.
- **E7 record-as-unit comparator (added 2026-07-30, panel S1-13):** the record-level certifier (per-record atoms, M=1, 2000-record subsample) grants α=0.05 in 99–100% of the draws the site-unit walk refuses in 100%, then violates its aggregate budget in 3.5% (s_u=0.5) / **9.6%** (s_u=2.0 — ~2δ) of them; at α=0.10 it deploys laxer thresholds under heterogeneity (mean τ 0.587 vs 0.747, fresh-pool R_M 0.081 vs 0.052). The motivating record-level failure is now demonstrated in-harness, not cited.

## How to run

```bash
pip install -r requirements.txt
python -m pytest tests -q                       # ~25s, must stay green
python -m experiments.run_synthetic --quick     # smoke grid (R=10)
python -m experiments.run_synthetic             # full paper grid (R=200); E4 is the long pole
```
Experiments write CSVs, PNGs, and `summary.md` to `experiments/out/`.

The eICU real-data path (mock corpus stands in until the credentialed extract lands):

```bash
python -m experiments.eicu_mock --out ./eicu-mock                      # 9k stays / 180 hospitals
python -m experiments.run_eicu --data ./eicu-mock --preflight --no-reference-check
python -m experiments.run_eicu --data ./eicu-mock --replicates 2
```

Off-default arms: `CERTGATE_EICU=1` (208-hospital / 200,859-stay mock, ~2 min) and
`CERTGATE_EICU_LARGE=1` (900 hospitals — the only arm that reaches the CERTIFIED branch;
`margin_floor` scales as `1/n_carrying`, so the mock's decline is a property of the frozen
corpus sizes, **not** of "any corpus size" — the crossing point is `n_carrying = 77`).
`--preflight` profiles and writes the a-priori predictions but certifies nothing; drop
`--no-reference-check` for the real extract so a wrong download raises. Outputs are
`EICU_*` + `EICU-SUMMARY.md` in `experiments/out/` and are **aggregate-only by
construction** (`run_eicu.assert_aggregate_only` gates every write).

## Invariants — do not break these

- **`SPEC.md` is the binding contract.** Change it *first*, then the code. Its "Audit-lesson conformance" checklist must stay satisfied.
- **Frozen constants are pinned literally** by `tests/test_constants.py`. Any drift must fail CI. This is the lightweight pre-registration substitute — treat a red constants test as a design change, not a nuisance.
- **All third-party imports at module top level** (never inside functions) — enclave/reproducibility requirement.
- **Input validation is loud and at the boundary** (`certgate/validate.py`): strict bool labels, finite features/weights/scores, dense site IDs, site-disjointness assertion. `require_both_classes=False` is the *only* sanctioned relaxation, and only for target pools (an all-negative deployment batch is legitimate at ~9.5% prevalence).
- **The certified path validates its guarantee text** — the guarantee statement in `report.py` must keep the site-population-average estimand / between-site-dispersion / not-a-realized-count / baseline-only-shared-event / operative-rung-selection / concept-out-of-scope / BBSE-four-parameter-box clauses (2026-07-25 audit V1/V3/V13/V27 wording — the old "per-target-site" claim was FALSE and must never return). The exact string is frozen by `tests/test_report.py`; changing it is a SPEC change.
- **Determinism:** every experiment seeds from `constants.SEED`; identical inputs → byte-identical certificates. Preserve this.
- **The eICU extract is never committed and never redistributed.** PhysioNet DUA 1.5.0 restricts *derived record-level* artifacts too, so `.gitignore` denies `*.csv.gz` and the `eicu-*/` corpus directories by default, and every `run_eicu` write passes `assert_aggregate_only` (`EICU_FORBIDDEN_OUT_KEYS`, arrays capped at 512). `eicu_etl.py` is **stdlib + numpy only** and `eicu_mock.py` is **stdlib only** — no pandas, no pyarrow (audit F16); a source grep enforces it.

## Gotchas

- `experiments/run_synthetic.py --only <E>` merges: the summary writer preserves existing `summary.md` sections for experiments it did not recompute (parsed from the `## E<n>` + fenced-json block format — see `_existing_summary_blocks`). Hand-edits to `summary.md` outside that exact format will not survive a partial rerun.
- E4 (site sweep to 400 sites × 200 draws) is by far the heaviest experiment; run it in the background and confirm the process is alive rather than trusting a run started across a session boundary.
- `run_eicu.py` owns `EICU-SUMMARY.md` and must **never** write `summary.md`: `run_synthetic._existing_summary_blocks`' regex is `^## (E\d)` — a *single* digit — so an `## EICU-…` section placed there is unparseable and gets silently clobbered on the next partial `--only` rerun.
- **The mock corpus cannot certify, by arithmetic, at any size.** At the frozen `EICU_MOCK_SIGNAL_B = 0.85` the outcome's Bayes-optimal AUC is 0.73, the fitted head reaches 0.60 out of sample, and the best achievable margin is 0.017 at α=0.10 against a `margin_floor(63, δ, 0.10)` of 0.043. `run_certgate` therefore declines every rung, so the suite exercises the **decline branch only** — `_eval_rung`'s certified path, `_abstention_ranking` (which settles P4), and the comparator's certified path stay unrun until the real extract arrives. Raising `EICU_MOCK_SIGNAL_B` toward `synth_fixture.SIGNAL_B = 2.0` is the one-constant fix and is a SPEC + `test_constants` decision, not a generator tweak.

## Files

**Panel S2 writing pass (2026-07-30):** S2-2 (three-level deployment rule — within-mode max coverage / across-mode most conservative / across-rung strictest α; the rules never conflicted, the paper just never said they act at different levels), S2-13 (Table 5 constants-justification + §4.9/Table 6 influence-cap sweep) and S2-28 (§3.1 clinical-target/loss block + Table 7 operating characteristics) are written and closed, plus S2-25 and the `DS-45`/`R1-13`/`R2-27` settles. Corrected along the way: §3.6's "each at full δ" (baseline spends `DELTA`, BBSE spends `BBSE_DELTA_BET`). Tables 6 and 7 come from `experiments/panel_s2_tables.py` — read-only, hard-asserts its replay against the released `E1_validity.csv` — and are **not** yet wired into `run_synthetic.py`, so §3.10/A.3 now scope the one-command claim to Figures 1–7 and Tables 1–4. See `paper/TODO.md` §0-pre.

**eICU-CRD v2.0 real-data path (2026-07-30).** Protocol frozen *before* the extract was read — that ordering is the whole of its pre-registration value, so `EICU-PROTOCOL.md` and the `test_constants.py` eICU pins must not be edited to match whatever the data turns out to say. Cohort = first ICU stay per hospital admission, no LOS floor / no min-stays-per-hospital / no APACHE filter (each omission argued); label `hospitaldischargestatus == "Expired"`; **site = `hospitalid`**, 208 hospitals; 161 features from a deny-by-default allowlist with a 36-entry tested leak denylist; dual `-1`/`''` sentinel policy; imputation fit on S_train only. Two target arms (24 per-hospital K=1 pools + one pooled K=24), 7 a-priori predictions P1–P7 and 5 pre-declared failure criteria F-A–F-E evaluated in code.

**2026-07-31 ingest audit — one critical, six major, all fixed (protocol amendments A1–A4, threats T-20–T-25).** The critical: **APACHE missingness is jointly site- AND *outcome*-informative**, because the day-1 window does not close for a stay that ends *because the patient died* — so `aps_present`/`apv_present` and the 43 `__missing` siblings are a partial outcome proxy **with no column name**, invisible to the 36-entry denylist, the `-1` gate, the drift gate, and the old α/coverage-conditioned F-D (a planted leak certified α=0.10 at coverage 0.86 and F-D reported `fired: false`). Worse, **P4 pre-registered the leak's signature as a confirmation**. Now: measured (`outcome_stratified_missingness`, `apache_absent_los`, `n_positive` on every attrition step), aborted (`outcome-informative-missingness` at `EICU_MAX_OUTCOME_PREVALENCE_RATIO = 2.0`), alarmed at runtime (F-D rewritten into three legs; two depend on neither α nor coverage and run every replicate on the real extract), and escaped explicitly by the new `--arm apache-linked`, which pays the immortal-time cost out loud. Also fixed: the mock leak probe's ceiling (0.98 → **0.80**, derived from the mock's own stated Bayes-optimal 0.726) plus a subtle cell-level positive control; reference-identity counts taken at S0 (the old post-filter count would have aborted the mandatory arrival-day `preflight` on the **correct** extract); typed read-boundary errors (`undecodable-table`/`truncated-table`); `unrecognised-null-token` (a Postgres backslash-N re-export silently zeroed 86 of 161 coefficients); `duplicate-stay-id` (the two cohort scans disagreed about which duplicate wins); preflight no longer raises on the drift it exists to profile; decidable `header_case_as_read`; lower-**closed** `fio2` windows (room air is an observation, not a missing value); `outcome_screen` settles the nine timing-unverified `apachePredVar` flags (`activetx` first) from data rather than DDL comments; and `EICU_MOCK_SIGNAL_LOAD` is now pinned (it could be zeroed to a pure-noise outcome model with the suite fully green — exactly the audit-V7 failure its own docstring cites).

**One finding is NOT fixed and needs the user: the project directory is not a git repository.** `git rev-parse --is-inside-work-tree` fails, so `.gitignore` is inert, nothing is tracked or ignored, two operator-checklist steps error, and §0's pre-registration ordering claim has **no timestamp of any kind**. `EICU-PROTOCOL.md` §0 and §14.1 step 0 now state this plainly and carry the exact `git init` + freeze-commit commands; run them **before** the extract is downloaded, or soften the paper's ordering claim.

`README.md` scope & objectives · `METHODS.md` paper-ready methods · `SPEC.md` engineering contract · `EICU-PROTOCOL.md` frozen real-data protocol (cohort, allowlist, denylist, predictions, threats T-1–T-25) · `PAPER-OUTLINE.md` venue fit + reviewer-risk table + timeline · `REDTEAM.md` internal red-team report + resolution · `certgate/` package · `tests/` · `experiments/` (`eicu_mock.py` mock corpus · `eicu_etl.py` extract→cohorts · `run_eicu.py` runner).
