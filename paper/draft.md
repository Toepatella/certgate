# CertGate: finite-sample certified selective prediction for multi-site clinical risk models, with label-shift robustness and explainable abstention

[AUTHOR NAME(S)]$^{1}$ [· ORCID]

$^{1}$ [AFFILIATION — department, institution, city, country]

**Corresponding author:** [NAME], [EMAIL]

---

**Abstract** Clinical risk models are deployed across many sites, yet the records they score are not independent: they cluster by site, so a confidence guarantee written at the record level is silently overconfident once deployment spans institutions. We present CertGate, a selective-prediction gate that treats the site, not the record, as the unit of independence and certifies, with finite-sample confidence $1-\delta$, that the error rate among answered cases — averaged over the site population calibration sampled — stays at or below $\alpha$. That average does not bound individual sites, which can exceed $\alpha$ at an unmeasured rate. The method rests on two moves: an influence-weighted answered-set risk certified by a betting martingale over sites, and a label-shift mode whose black-box correction carries all four estimated parameters' uncertainty inside the guarantee. Because the deployed risk model is linear, every answer and abstention carries an exact feature attribution, so the gate explains abstentions as well as answers. On a 208-site synthetic cohort, at $\alpha=0.10$ the gate certifies all 200 calibration draws at 0.983 mean coverage with no exceedance of the certified quantity on fresh pools, inside the $\delta=0.05$ budget. Under a prevalence shift from 0.095 to 0.22, the uncorrected baseline certifies and violates in 39.5% of draws, while the corrected mode declines all 200 rather than certify. A concept-shift negative control, out of scope for any unlabeled method, hard-violates 70% of draws, as the stated assumptions predict; a site-count frontier puts the stricter $\alpha=0.05$ budget near 300 sites, reliable by 400.

**Keywords** Selective prediction · Distribution-free uncertainty quantification · Cluster-robust inference · Label shift · Explainable abstention · Clinical risk prediction

---

# 1 Introduction

A clinical risk model rarely stays where it was built. A deterioration or readmission score calibrated on patients from a handful of academic centers is deployed at a community hospital that contributed no training records, and in deployment the model does not hesitate: it emits a probability for every patient, and downstream software turns that probability into an action. A safer design wraps the model in a *selective gate* that answers the cases it is confident about and abstains on the rest, routing the hard ones to a clinician [@chow1970reject; @elyaniv2010selective]. The promise attached to such a gate is a bound on the error rate among the cases it chose to answer, holding with high confidence — what a clinician weighs before trusting an automated triage, and what an auditor asks to see documented. Under the way multi-site clinical data is actually distributed, that promise is often false, in a way the deployed system cannot see.

Three questions in reliable machine learning have mature but separate answers. *Selective prediction* asks when a model should decline: the reject-option lineage [@chow1970reject; @elyaniv2010selective] and its finite-sample form certify that the error rate among answered cases stays at or below a budget $\alpha$ with confidence $1-\delta$ [@geifman2017selective], a guarantee recent work has sharpened with e-values and adaptive thresholds [@score2026; @yu2026joint]. *Distribution-free inference* asks how a guarantee can hold when the sampling unit is a cluster rather than a record: hierarchical conformal methods deliver coverage whose unit of independence is the group [@dunn2023hierarchical; @lee2025hierarchical]. *Label-shift estimation* asks how to keep a guarantee valid when outcome prevalence moves between calibration and deployment, and how to fold the estimation error of that correction back into the stated confidence [@lipton2018bbse; @podkopaev2021labelshift; @si2024pac]. Each literature is deep, but multi-site clinical deployment needs all three at once: a certified answered-set risk, computed with the cluster as the unit of independence, and robust to label shift.

Multi-site clinical risk prediction sits exactly there. The object to certify is an *answered-set error rate* whose unit of independence is the **site** (hospital), not the record: records within a site share case mix, coding conventions, and instrumentation, so they are not independent draws, and the prevalence a new site presents is generally not the one calibration saw [@tripodcluster2023; @internalexternal2021]. A certificate that treats records as exchangeable overruns its stated confidence once deployment is grouped by site — a failure recently documented for record-level selective-risk rules under grouped deployment [@zhou2026falsesense] and prevalence shift [@triage2026audit].

We present CertGate, a selective-prediction gate for this setting. The certificate treats the site as the atom of inference: influence-capped per-site statistics feed a betting-martingale (WSR) sequential test [@waudbysmith2024betting; @ville1939], and a fixed-sequence threshold walk selects the operating point without spending confidence on multiplicity [@angelopoulos2021ltt; @westfall2001fixedsequence], in the risk-control tradition [@bates2021rcps; @angelopoulos2024crc]. It ships in two assumption modes, and the mode is part of the guarantee text: an exchangeable baseline that assumes calibration and deployment sites share outcome prevalence, and a label-shift mode that drops that assumption through a black-box correction [@lipton2018bbse; @garg2020unified] and propagates the estimator's own uncertainty into the certificate through a cluster-robust box over all four estimated parameters, following the weight-uncertainty-in-the-guarantee pattern of [@si2024pac]. Three of those four intervals are percentile bootstraps — asymptotic, and measurably under-covering at this design's cluster count — which we disclose wherever the guarantee is stated. Because the deployed head is linear by design, every answer and every abstention carries exact linear attributions [@lundberg2017shap], attaching explanation to a certified decision rather than to an ungoverned reject option [@artelt2022reject; @ifac2025abstainexplain]. One assumption stays deliberately outside the guarantee: concept shift — a change in $P(y\mid x)$ — is undetectable from unlabeled data, so we do not claim to survive it.

On a synthetic cohort of 208 sites whose lognormal sizes, $\sim$9.5% prevalence, and site random effects follow the distributional profile reported for large multi-site clinical cohorts, the certificate is valid but not vacuous. At $\alpha = 0.10$ every one of 200 calibration draws certifies at $0.9828$ mean coverage, and the certified quantity — the influence-weighted answered risk averaged over a fresh 200-site evaluation pool — exceeds $\alpha$ on none of them, inside the $\delta = 0.05$ budget; the per-site dispersion the guarantee deliberately leaves unbounded is measured beside it. Under a site-level prevalence shift from $0.095$ to $0.22$, the uncorrected baseline certifies and hard-violates on $39.5\%$ of draws, whereas the label-shift mode, with the target predicted-positive rate's own uncertainty budgeted, issues no certificate at all on a single-site target pool of this size: it declines all 200 draws. A site-count sweep locates the feasibility frontier: $\alpha = 0.10$ is reliably certifiable from about 150 sites, while $\alpha = 0.05$ first appears near 300 and stabilizes only at 400, so at the 208-site scale $\alpha = 0.10$ is the operative rung — a property of how many hospitals contribute, not of the method.

Certified selective risk [@geifman2017selective; @yu2026joint], cluster-as-unit distribution-free inference [@dunn2023hierarchical; @lee2025hierarchical], and label-shift guarantees that carry weight-estimation uncertainty [@si2024pac] each exist separately; CertGate combines them into a single working certificate on the answered-set error rate. The primary contributions of this work are:

1. **A site-as-unit certified selective-prediction gate.** A finite-sample $(\alpha, 1-\delta)$ certificate that bounds the influence-weighted error rate among answered cases, averaged over the site population, with the site rather than the record as the unit of independence. This is the central contribution, and its necessity is demonstrated rather than assumed: on the same draws, the equivalent record-as-unit certifier grants the $\alpha = 0.05$ budget the site-unit walk refuses and then violates it at up to twice $\delta$ under between-site heterogeneity (E7), while the site-count frontier prices what honest certification costs (E1, E4).
2. **A label-shift mode that carries its own uncertainty.** A BBSE-based correction keeps the guarantee valid under outcome-prevalence shift by folding the estimation uncertainty of all four corrected parameters into the $1-\delta$ budget through a cluster-robust box, with the assumption mode named in the guarantee text — replacing a $39.5\%$-violation baseline with a gate that certifies or declines rather than issuing a certificate it cannot support, and which, on the small single-site target pools tested here, declines (E2).
3. **Exact explanations attached to every certified decision.** Because the deployed head is linear, each answer and each abstention carries exact additive attributions [@lundberg2017shap], including which features pushed a declined case below the confidence bar; the contribution is the attachment of explanation to a *certified* decision, extending reject-option explanation work that carries no statistical guarantee [@artelt2022reject; @ifac2025abstainexplain]. This supports the certificate above rather than standing as an independent method (E5).
4. **A validation design that can fail.** A concept-shift negative control verified capable of failing before it is run, and a protocol that scores the certificate against the estimand it actually claims — one conformance metric carrying the $\delta$ target, reported beside the per-site dispersion and realized-exceedance diagnostics that deliberately carry none — the rigor that constructively answers a published diagnosis of overconfident selective certificates [@zhou2026falsesense], rather than a new validation philosophy (E3).

# 2 Related work

CertGate draws on four literatures that each solve part of the problem and stop one axis short. We take them in turn, closing each with the nearest work and the gap between what it guarantees and what CertGate does.

## 2.1 Certified selective prediction

The reject option dates to Chow [@chow1970reject]; El-Yaniv and Wiener [@elyaniv2010selective] formalized the risk–coverage tradeoff, and Geifman and El-Yaniv [@geifman2017selective] turned it into a finite-sample selective-risk certificate on i.i.d. records. The distribution-free risk-control tradition sharpened it — RCPS for high-probability $(\alpha,\delta)$ control [@bates2021rcps], Learn-then-Test [@angelopoulos2021ltt], and Conformal Risk Control (in expectation only) [@angelopoulos2024crc] — and a 2025–26 wave returns to selective risk [@scrc2025; @score2026; @yu2026joint]. Yu and Liu [@yu2026joint] are closest: the same certificate shape — a selected-risk bound with an acceptance floor and a decline option — but over i.i.d. records, whereas CertGate makes the site the unit of independence and carries a label-shift correction inside the budget.

## 2.2 Conformal prediction and cluster exchangeability

Conformal prediction [@vovk2005algorithmic] gives distribution-free coverage under exchangeability of the calibration and test points — false under multi-site clustering, where records within a hospital are not exchangeable with those from an unseen site. Applied at the record level it certifies the wrong estimand (per-record coverage, counting correlated records as fresh evidence); lifted to the cluster level it fixes the unit but still yields coverage, not a risk budget on the answered subset. Dunn, Wasserman and Ramdas [@dunn2023hierarchical] and Lee, Barber and Willett [@lee2025hierarchical] supply the cluster-as-unit machinery CertGate reuses, and Shahid [@fedcrc2026] makes sites first-class in a federated risk control — but the first two guarantee coverage, not selective risk, and the third controls risk only in expectation. CertGate reuses the cluster unit to certify an answered-set risk budget.

## 2.3 Label shift

Black-box shift estimation [@lipton2018bbse] corrects a moving prevalence by confusion-matrix inversion; regularized [@azizzadenesheli2019rlls] and unified [@garg2020unified] variants refine the weights, and Podkopaev and Ramdas [@podkopaev2021labelshift] give reweighted-conformal coverage — all record-level point corrections. Si et al. [@si2024pac] are nearest, propagating confusion-matrix uncertainty into the guarantee rather than treating the correction as exact; CertGate adopts that pattern for a bound on answered-set risk, with the uncertainty made cluster-robust by a bootstrap over sites.

## 2.4 Multi-site clinical validation and explainable abstention

Clinical practice already treats the site as the unit — TRIPOD-Cluster [@tripodcluster2023] and internal–external cross-validation [@internalexternal2021] — but reports point estimates, not certificates. Two recent audits sharpen the gap: certified record-level selective-risk rules overrun their budget by 9–30% under grouped deployment [@zhou2026falsesense], and marginal coverage cannot certify a grouped release under prevalence shift [@triage2026audit]. Reject-option explanation methods [@artelt2022reject; @ifac2025abstainexplain; @l2lore2025] attach no statistical guarantee to the decision; CertGate's difference is exact attributions on a certified gate. The same certificate shape appears in power-grid contingency screening [@thermal2026audit], so it is not specific to medicine.

# 3 Methods

## 3.1 Problem setting: the site as the unit of independence

We consider multi-site clinical data in which on the order of two hundred collection sites each contribute between 20 and 5,000 records, the outcome prevalence is roughly 9–10%, and sites differ in both prevalence and case mix. A deployed risk model produces a bounded score $s(x) \in [0.5, 1]$ — the max-softmax of a probabilistic classifier over the binary outcome $y \in \{0,1\}$ — and answers a case only when that score clears an operating threshold $\tau$; otherwise it abstains and defers the case to human judgment. The object we want to construct is a *certificate*: with probability at least $1-\delta$ over the draw of calibration sites, the influence-weighted answered-set risk — the parameter $R_M$ defined in Section 3.3 — *averaged over the population of sites from which the calibration sites were drawn* is at most $\alpha$, under a stated and explicitly tagged assumption about how deployment differs from calibration. This is a site-population average, not a per-site promise: under between-site heterogeneity an individual site's answered risk can exceed $\alpha$ while the average stays inside budget, at a rate the certificate neither measures nor bounds (Section 3.7 states the full clause set, and Section 4.2 measures the dispersion). This is selective-risk control in the reject-option lineage, and the phrasing "answered-set risk $\le \alpha$ with probability $1-\delta$" follows the convention established for distribution-free selective classification [@geifman2017selective].

**The clinical target, the decision rule, and the loss.** Because the certified quantity is an *error* rate, its meaning is fixed by three things the deployment supplies and the method takes as given. *The outcome.* The binary label $y \in \{0,1\}$ must be pinned by an index event (when a case enters the gate), a prediction time at which the features $x$ are frozen, and an ascertainment window over which $y$ is resolved — for a deterioration score, for instance, admission as the index event, a fixed number of hours after admission as the prediction time, and in-hospital death or unplanned ICU transfer within that admission as the outcome. CertGate certifies whatever binary label the caller supplies; it does not itself resolve censoring, competing risks, or a time-to-event horizon, so a label that cannot be ascertained for every record in the calibration and target pools is outside what the certificate speaks to. *The decision.* The head's hard label is $\hat{y}_i = \mathbb{1}[\hat{p}(x_i) \ge 1/2]$ and the per-record error indicator is $\text{err}_i = \mathbb{1}[\hat{y}_i \neq y_i]$ — the quantity that enters the atom of Section 3.3. The selective score $s(x) = \max(\hat{p}(x), 1 - \hat{p}(x))$ is symmetric about $\hat{p} = 1/2$, so the gate withholds cases near that decision boundary in either direction and introduces no clinical decision threshold of its own. *The loss.* $\text{err}_i$ is symmetric 0–1: a false negative and a false positive contribute equally to $R_M$, and $\alpha$ budgets their sum. This is a modeling choice and not a neutral one. At single-digit prevalence most answered errors are false negatives — 77% in E1 (Section 4.2) — which in most clinical framings are the costlier kind; and an error budget set near the outcome prevalence is close to what a constant always-negative rule already achieves, so meeting the budget is not on its own evidence that the gate is doing anything. The answered-set operating characteristics of Section 4.2 and the composition instrument of Section 3.8 exist to make that visible. A cost-weighted loss would require a different atom and is not offered here (Section 6.1).

The decisive modeling choice, argued in Section 1, is the unit of independence: the site (cluster), not the record. Every guarantee below is a cluster-level guarantee resting on roughly eighty site-level observations, not the $\sim\!10^5$ record-level ones a naive analysis would claim. Cluster-as-unit distribution-free inference has precedent in the conformal literature [@dunn2023hierarchical; @lee2025hierarchical]; we adopt it as the foundation of the certificate.

## 3.2 Data discipline and lightweight pre-registration

Sites are partitioned disjointly into three pools by site count: $S_{\text{train}}$ (40% of sites, used to fit the risk model), $S_{\text{aux}}$ (20%, used for threshold ordering, the label-shift correction machinery, and feasibility diagnostics), and $S_{\text{cal}}$ (40%, reserved for certification). $S_{\text{cal}}$ enters the certified path only through the certification walks; the estimated and diagnostic tiers of the report also read it, but strictly downstream of the certificate and with no feedback into it. Every fitted object — the head, the walk order, the confusion-matrix statistics — is a function of $(S_{\text{train}}, S_{\text{aux}}, \text{target features})$ alone; the calibration pool contributes nothing until the certificate is computed. The pipeline enforces split disjointness with a hard assertion at entry, so any accidental leakage of sites across pools aborts loudly rather than inflating a certificate. This site-disjoint discipline mirrors internal–external cross-validation practice for multi-site clinical prediction models [@internalexternal2021].

All constants that govern the procedure are fixed a priori and pinned to their literal values by a unit test, so any drift fails continuous integration. This is a lightweight, machine-verifiable discipline that pins the constants against drift; it is not third-party pre-registration, since the freeze date is not externally attested — and pinning a value is not the same as justifying it. Table 5 therefore lists every frozen constant with its role and the basis on which the value was set, including the four that govern gating and numerical hygiene and are easy to miss in prose: the record-carrying calibration-cluster floor (50), the target-pool floor below which the gate declines outright (10 records), the implied-prevalence clip ($10^{-4}$), and the standardization tolerance ($10^{-9}$). The one constant whose value materially moves the certificate is the influence cap $M$; Section 4.9 reports its sensitivity directly.

## 3.3 The certified estimand: influence-weighted answered-set risk

Because site sizes are heavy-tailed, an unweighted mean of the answered-set error over records would let a single large site dominate the certified quantity. The natural fix — capping each site's *realized* contribution — is provably anti-conservative: a construction with 17.5% true risk certifies at $\alpha = 5\%$ under naive truncation (Appendix A.3; retained as a regression test). Instead, each site receives a *data-independent* influence weight $g_c = \min(n_c, M)$ with $M = 100$, and the certified quantity is the influence-weighted answered-set risk

$$R_M = \frac{\sum_c g_c\, a_c\, e_c}{\sum_c g_c\, a_c},$$

where $a_c$ is the answered fraction and $e_c$ the answered-set error rate at site $c$. The cap acts on influence only: it scales each site's whole signed contribution and never censors the error itself, so a site that answers many cases badly still enters with its full adverse error rate $e_c$.

What $M = 100$ buys, and what it costs, should be stated plainly rather than left implicit in the formula. On this cohort's size distribution $89.5\%$ of sites exceed 100 records, so $g_c$ is pinned at $M$ for roughly nine sites in ten and $R_M$ is close to an unweighted average over sites rather than over records; $86\%$ of calibration records sit above their own site's cap and carry no marginal influence, and the realized weight-per-record $g_c/n_c$ bottoms out at $1/50$ at the generator's 5,000-record clip. The consequence is direct: a 5,000-record hospital and a 100-record one enter the certified quantity with identical total influence. That is the intended behavior — it is what makes the certified quantity cluster-robust rather than dominated by whichever sites happen to be largest — but it is a substantive choice about whose errors count, not a technical detail, and Section 4.9 measures what the certificate does at other values of $M$.

Certification proceeds through the linearized per-site atom

$$Z_c = \frac{g_c}{M\, n_c}\sum_{i \in c} \text{ans}_i\,(\text{err}_i - \alpha) + \alpha \;\in\; [0, 1],$$

constructed so that $\mathbb{E}[Z] \le \alpha \iff R_M \le \alpha$. The bridge is a one-line identity: the inner sum collapses to $n_c a_c (e_c - \alpha)$, so $Z_c - \alpha = g_c a_c (e_c - \alpha)/M$, tying each atom to the estimand term by term — the atom mean sits above or below $\alpha$ exactly as $R_M$ does. Sites with no answered-eligible records enter as *neutral* atoms $Z_c = \alpha$ rather than being dropped. Dropping them would not change $R_M$'s numeric value — with $a_c = 0$ both of that site's terms vanish — but it would change two things that matter. It would narrow the population the guarantee refers to, from the sites calibration sampled to the subset of them that happened to answer, a set determined after the threshold is fixed. And it would change the test: a neutral atom contributes a wealth factor of exactly one, but it still occupies a position in the fixed sequence and still counts in the $n$ that sets the bet schedule $\lambda_t$ of Section 3.4, so it dilutes power without touching validity. The minimum-cluster gate that governs feasibility counts only *record-carrying* sites, so an empty site never pads the effective sample size; a site that carries records but answers none does count toward that gate and does enter as a neutral atom. In the experiments reported here that case never arises — across E1's 200 draws and E6's deployment, zero record-carrying calibration sites answered nothing at the deployed threshold — so the neutral-atom path is specified and exercised by the test suite but not by these results.

The expectation that defines the estimand is over the *site draw*: the index $c$ ranges over the population of sites from which the calibration sites are drawn, so $R_M$ is a population aggregate and $\mathbb{E}[Z] \le \alpha$ says that the influence-weighted answered risk *averaged over that population* is within budget. What follows from this — a population-average rather than per-site scope, the between-site dispersion the average does not control, a bound on the error *parameter* rather than on any batch's realized error *count*, and what the shared $1-\delta$ event does and does not cover — is stated once, consolidated, in Section 3.7.

## 3.4 The certification test: betting on the calibration sites

We test the null $H_0: \mathbb{E}[Z] \ge \alpha$ with the Waudby-Smith–Ramdas betting martingale for means of bounded random variables [@waudbysmith2024betting]. Processing the calibration sites in a fixed order, the wealth process is

$$K_t = \prod_{s \le t}\bigl(1 + \lambda_s(\alpha - Z_s)\bigr), \qquad \lambda_t = \min\!\left(\sqrt{\frac{2\ln(1/\delta)}{\hat{\sigma}^2_{t-1}\, n}},\; \frac{0.9}{1-\alpha}\right),$$

with a variance floor of $10^{-8}$ and the running mean and variance $(\hat{\mu}, \hat{\sigma}^2)$ initialized at $(0.5, 0.25)$. We certify if and only if $\sup_t K_t \ge 1/\delta$.

This is a finite-sample, level-$\delta$ test. Under $H_0$ the bet $\lambda_t$ is predictable and capped so every wealth factor stays positive, making $K_t$ a nonnegative supermartingale, and Ville's inequality bounds the probability it ever reaches $1/\delta$ by $\delta$ [@ville1939] — no asymptotics, and no distributional assumption beyond the boundedness of $Z_c$. Because the atom identity of Section 3.3 ties $Z_c$ to its site's term in $R_M$, testing $\mathbb{E}[Z] \ge \alpha$ stands in exactly for a test of the site-population $R_M \ge \alpha$. Appendix A.1 gives the four steps — boundedness, null equivalence, the supermartingale property, and Ville's inequality — in full. The processing order is a deterministic permutation of the calibration sites seeded by the SHA-256 of the protocol seed, the $\alpha$ index, the mode index, and — for the BBSE endpoint walks only — an endpoint discriminator. It deliberately carries no target identifier, so it preserves predictability, cannot be chosen to flatter the outcome, and makes the baseline certified thresholds one shared function of the calibration draw.

At $n$ clusters no test can certify with an arbitrarily small margin. We summarize this with an *information floor* $\ln(1/\delta)(1-\alpha)/n$ — a linearized, zero-variance lower bound used strictly as a feasibility diagnostic, never a gate. Because it scales as $1/n_{\text{cal}}$, it sets the capacity frontier the site-sweep experiment (E4) maps.

## 3.5 Threshold selection and the budget ladder

The operating threshold is chosen from a grid of 23 values evenly spaced in $[0.55, 0.99]$. Rather than split $\delta$ across all 23 candidates, we walk the grid in a fixed sequence: the thresholds are ordered by their estimated certification margin on $S_{\text{aux}}$, most conservative first, each is tested at the mode's full betting budget ($\delta$ for the baseline, $\delta_{\text{bet}}$ in the label-shift mode of Section 3.6) with no splitting across the grid, and the walk stops at the first failure. Stopping at the first failure makes the walk a fixed-sequence, learn-then-test procedure — the order is learned on $S_{\text{aux}}$ and risk is controlled on $S_{\text{cal}}$ — so no $\delta$-splitting across the grid is needed [@westfall2001fixedsequence; @angelopoulos2021ltt; @bates2021rcps].

Selection then happens at three levels, and stating them separately removes an apparent conflict between the rules. *Within a mode*, we deploy the maximum-coverage threshold in the certified prefix — operationally, the lowest $\tau$ the walk certified. No pool is named for that comparison because none is needed: the answered set $\{x : s(x) \ge \tau\}$ is nested and decreasing in $\tau$, so the lowest certified threshold maximizes coverage on every pool at once, and the rule is a property of the grid rather than a measurement. (The coverage *figures* in Section 4 are a separate matter: they are measured on the target pool the certificate is applied to, after $\tau^*$ is fixed, and play no part in choosing it.) *Across modes*, we deploy the most conservative certified threshold — the largest of the modes' own within-mode choices — so the deployed threshold is never laxer than any certifying mode would have deployed alone (Section 3.6). *Across rungs*, the operative $\alpha$ is the strictest certified one.

Both budgets $\alpha \in \{0.05, 0.10\}$ are certified by separate walks and reported as a coverage-versus-$\alpha$ curve. That last rule makes the operative rung a data-driven selection over the two-rung ladder: each rung is individually valid at $1-\delta$, and the selected claim holds jointly at $1-2\delta = 0.90$, as the guarantee text states (Section 3.7). Because the information floor scales as $1/n_{\text{cal}}$, we expect $\alpha = 0.10$ to be the operative rung at the 208-site scale (about 83 calibration clusters) and $\alpha = 0.05$ to require more sites; experiment E4 maps this frontier.

## 3.6 Assumption modes

**Baseline (exchangeable).** The target site is assumed exchangeable with the calibration sites and all record weights are set to one. Tag: "if exchangeability holds."

**Label shift (BBSE).** This mode has three moving parts — a point correction of the prevalence, an uncertainty box around that correction, and a certification that tests the box at its two endpoints — and we take them in turn. Only the outcome prevalence is allowed to differ between target and calibration; the class-conditional feature distribution $P(x \mid y)$ is held invariant. We estimate the classifier's confusion rates $c_0 = P(\hat{y}=1 \mid y=0)$ and $c_1 = P(\hat{y}=1 \mid y=1)$ on $S_{\text{aux}}$, estimate the target *population* predicted-positive rate $q$ by its observed value on the target pool, and invert the black-box shift equation $q = c_0(1-\pi_t) + c_1\pi_t$ for the target prevalence $\pi_t$ [@lipton2018bbse; @garg2020unified]. Certification then reweights each calibration record by class, $w_i = \rho$ for $y_i = 1$ and $w_i = 1$ for $y_i = 0$, where $\rho$ is the target-to-source odds ratio of the positive class.

Estimation uncertainty is carried inside the guarantee for all four estimated parameters rather than assumed away, following the pattern of propagating weight uncertainty into a distribution-free risk guarantee under label shift [@si2024pac]. The rate observed on a finite target pool is a noisy estimate of the population $q$, so $q$ receives its own confidence share alongside $(c_0, c_1, \pi_{\text{source}})$: a cluster bootstrap over the $S_{\text{aux}}$ sites supplies percentile intervals for the first three, while $q$ takes an exact two-sided Clopper–Pearson interval on a single-site target pool and a cluster bootstrap over target sites on a pool declaring ten or more. The four intervals are held at Bonferroni level $\delta_{\text{conf}}/4$ and form a box propagated to a weight interval $[\rho_{\text{lo}}, \rho_{\text{hi}}]$ over its sixteen corners; certification then tests both endpoints at $\delta_{\text{bet}}$. The resampling protocol and the corner-interval coverage argument that makes the endpoints sufficient are in Appendix A.2.

Testing only the two endpoints is sound because the certifiable set $\{\rho : \mathbb{E}[Z(\rho)] \le \alpha\}$ is convex — the quantity whose sign decides certification is affine in $\rho$ — so certifying at both endpoints covers every interior $\rho$. Appendix A.2 gives the argument, including the kink at $\rho = 1$ introduced by the per-endpoint normalization $w_{\max} = \max(1,\rho)$ and its numerical check in the test suite.

The two confidence budgets combine as $\delta_{\text{conf}} + \delta_{\text{bet}} = \delta$ with $\delta_{\text{conf}} = \delta_{\text{bet}} = 0.025$; the union bound restores $1-\delta$, where $\delta_{\text{conf}}$ is spent across the four-parameter box — over the $S_{\text{aux}}$ split and the target pool, not over the calibration draw — and $\delta_{\text{bet}}$ on the betting test. We disclose plainly what that box does and does not deliver. The $q$ interval is finite-sample for a single-site pool, but the three percentile bootstrap intervals are asymptotic, and their finite-cluster error is material at this design's own scale: holding the head fixed so $(c_0, c_1, \pi_s)$ are exact population constants, we measure the realized joint miscoverage of that three-parameter box at $0.038$ against a nominal $\le 0.019$ (joint coverage $0.962$ against $0.981$ nominal; per-parameter misses of roughly $0.010$–$0.015$ against a nominal $0.003$). Every BBSE guarantee statement carries the asymptotic caveat, and the printed confidence should be read with that measured shortfall in mind; replacing the percentile box with a finite-sample cluster bound is future work.

The BBSE mode *declines* rather than falls back whenever its assumptions or its inputs cannot support a correction. It declines on an empty target pool; on a target pool declaring between two and nine sites, because a percentile bootstrap over so few clusters cannot approach nominal coverage; on fewer than 2,000 valid resamples within 4,000 attempts, a degenerate bootstrap pool for which we refuse to take quantiles over a silently reduced draw count; on a worst-case confusion gap $(c_1 - c_0) < 0.10$, an ill-conditioned inversion; and when the $q$ interval is not wholly inside the box's $[c_{0,\text{lo}}, c_{1,\text{hi}}]$ range, meaning the implied prevalence leaves $(0,1)$ and BBSE is misspecified. There is no shrinkage and no fallback to the uncorrected baseline anywhere: a decline is the only alternative to a certificate.

**Combination.** The modes run as alternatives rather than in sequence, each spending its own betting budget — the baseline walk the full $\delta$, the label-shift walk $\delta_{\text{bet}}$ — and we deploy the most conservative certified threshold, meaning the largest of the two modes' within-mode choices. The two selection rules of Section 3.5 act at different levels and do not conflict: each mode first takes the maximum-coverage threshold inside its own certified prefix, and the combination then takes the more conservative of those, so the deployed threshold is never laxer than either certifying mode would have deployed on its own. A mode is listed in the combined guarantee only if its own certified prefix contains the deployed threshold, so the OR-guarantee reads "if *either* tagged assumption holds…" and agreement between modes is not treated as extra confirmation.

**What no mode covers.** Concept shift — a change in $P(y \mid x)$ — is invisible to any method that sees only unlabeled target data, and under it the certificate can be confidently wrong. We do not paper over this. Experiment E3 injects a concept tilt that is first *verified* capable of pushing the mean realized answered error above $\alpha$ at certifiable coverage and then shown to make the certificate fail, demonstrating that the assumption tags do real work, and marking the boundary of the method.

## 3.7 The certified guarantee

The clauses below are the deployed guarantee text: the certificate emits them, and they are frozen as exact strings per mode combination, so weakening or strengthening one is a specification change rather than an editorial one.

(1) *Estimand.* Under exchangeability the certificate bounds the influence-weighted answered-set risk averaged over the population of sites from which the calibration sites were drawn, with the probability taken over the calibration draw; under label shift it bounds that same site-population risk reweighted to the target class prevalence the BBSE correction identifies, with the probability taken over the joint draw of the calibration sites, the auxiliary split, and the target pool. (2) *Dispersion.* This bounds a site-population average and not any individual site's answered error rate: under between-site heterogeneity individual sites can exceed $\alpha$ while the average stays within budget, at a rate the certificate does not measure or bound. (3) *Parameter, not count.* It does not bound any batch's realized error count, which exceeds $\alpha$ at binomial-dispersion rates even under a valid certificate — at the $\alpha$ boundary approaching one batch in two as batches grow. (4) *Shared event, baseline rows only.* In the exchangeable mode the certified thresholds are a function of the calibration draw alone, so one $1-\delta$ event is shared by every target pool the certificate is applied to; the clause is omitted wherever BBSE covers the row, because the BBSE fit depends on the target pool through $q$ and no shared event exists there. (5) *Operative rung.* The operative rung is the strictest certified $\alpha$, a data-driven selection over the $\{0.05, 0.10\}$ ladder, so the selected claim holds jointly at $1 - 2\delta = 0.90$. (6) *Concept shift.* Concept shift and combined shift are out of scope and undetectable from unlabeled data; the certificate is void there. (7) *BBSE rows add.* The $[\rho_{\text{lo}}, \rho_{\text{hi}}]$ box covers four estimated parameters $(c_0, c_1, \pi_{\text{source}}, q_{\text{target}})$ at Bonferroni $\delta_{\text{conf}} = 0.025$, spent over the auxiliary split and the target pool rather than the calibration draw; the $(c_0, c_1, \pi_{\text{source}})$ intervals are percentile cluster bootstraps — asymptotic, with realized joint coverage measurably below nominal at small cluster counts (Section 3.6) — while the $q_{\text{target}}$ interval is finite-sample Clopper–Pearson for single-site pools and a cluster bootstrap for multi-site pools, with $\delta_{\text{bet}} = 0.025$ spent on the betting test.

## 3.8 Explainability: answers and abstentions

The deployed head is L2-regularized logistic regression with $C = 1.0$, chosen so that the answer/abstain decision is intrinsically interpretable. The gate itself is model-agnostic: the score only *ranks* cases, and the validity of the certificate never depends on the quality or calibration of the model producing that score. A stronger black-box head can be substituted at a visible cost in coverage; we use logistic regression here because the explainability requirement, not the certificate, calls for a transparent head.

Three artifacts make the gate explainable. *Globally*, the standardized coefficients — the coefficients learned on standardized inputs — give the direction and strength of each feature. *Locally*, for an answered case we report exact additive attributions $\phi_j(x) = w_j(x_j - \mu_j)$ with $\text{logit}(\hat{p}(x)) = \text{base} + \sum_j \phi_j$. Here $w_j$ is the raw-feature-space coefficient, $w_j = \text{coef}_j / \text{sd}_j$, where $\text{coef}_j$ is the coefficient learned on standardized inputs and $\text{sd}_j$ the training-split standard deviation of feature $j$; the standardized coefficient used literally would not sum to the logit, so the raw-space form is the one that makes the decomposition exact. For a linear model these attributions are exact Shapley values, with no approximation or sampling [@lundberg2017shap]. *For declined cases*, answering requires $|\text{logit}\,\hat{p}(x)| \ge \text{logit}\,\tau^*$, so we report the margin-to-answer $m(x) = \text{logit}\,\tau^* - |\text{logit}\,\hat{p}(x)| > 0$ together with the signed feature attributions of the confidence deficit — "declined because feature A pulls toward positive while features B and C pull toward negative, leaving confidence below the certified bar." At the cohort level, the attribution profiles of the answered and declined populations identify systematic abstention drivers. Explaining reject options is itself established [@artelt2022reject; @ifac2025abstainexplain]; what the gate adds is that the same exact attributions accompany a certified decision — a supporting capability the linear head makes nearly free.

A fourth artifact guards against a subtler failure. The answered-set class composition is reported three ways — the predicted-class positive fraction (estimated), the BBSE-implied true-class fraction (estimated, and tagged with its label-shift assumption), and the oracle true-class fraction (available in the synthetic harness only) — which together reveal whether a certificate was earned by answering only easy negatives on a task with 9–10% prevalence, rather than by controlling error on a representative mix.

## 3.9 Validation protocol

Validation, on the synthetic harness where oracle labels are available, has to score the certificate against the estimand it actually claims: scoring a population-average guarantee against single-site draws tests a claim the certificate does not make. It therefore turns on one conformance metric that carries the $\delta$ target and two diagnostics that deliberately do not, and the three must never be conflated. Small answered sets exceed $\alpha$ by chance at binomial rates, and at the boundary the exceedance rate approaches 50% as batches grow, so a naive count of pools with realized error above $\alpha$ would condemn even a perfectly valid system.

The metric with a target is **aggregate conformance**: for each calibration draw the certified threshold is applied to a fresh 200-site evaluation pool, the influence-weighted answered risk $R_M$ is computed on that pool, and we require the fraction of certified draws with $R_M > \alpha$ to stay at or below $\delta$, replicated over at least 200 independent calibration draws. This is the quantity the betting test bounds, and the calibration draw, not the target site, is the unit of replication, because it is the draw of calibration sites over which the $1-\delta$ event is defined.

The first diagnostic, carrying **no $\delta$ target**, measures the between-site dispersion the certificate declines to bound: the single-fresh-site **hard-violation rate** — a site counts as violated only when the one-sided 95% Wilson lower confidence bound on its answered error exceeds $\alpha$ [@wilson1927] — reported beside the mean fraction of evaluation-pool sites whose own answered error exceeds $\alpha$. Under heterogeneity these rise while the aggregate stays within budget, which is why no budget is attached to them. The second is the **realized exceedance rate**: the raw fraction of pools whose answered error exceeds $\alpha$, reported against its binomial reference at the $\alpha$ boundary and stratified by answered-set size bins $\{<30,\ 30\text{–}100,\ 100\text{–}300,\ >300\}$.

The Wilson criterion is deliberately conservative: it exists to protect a valid system from false condemnation, not to detect the smallest possible excess. The shift experiments (E2, E3) apply that same criterion to the target pool as their failure measure, which is the direction that criterion is safe in: there the question is whether a certificate is grossly wrong, and a conservative rule can only understate how often it is. Meeting the $\le \delta$ conformance target therefore evidences the *absence of gross violations at the tested power*, rather than confirming validity against arbitrarily small excesses. The guarantee itself rests on the finite-sample level of the betting test (Section 3.4), not on this harness.

## 3.10 Reproducibility

Every result regenerates deterministically from the released code: a single protocol seed drives the whole grid through a fixed hashing rule, the frozen constants are pinned by a unit test (Section 3.2), and one command reproduces Figures 1–7 and Tables 1–4, with identical inputs yielding byte-identical certificates. Tables 6 and 7 come from a second, read-only command that reseeds the same draws and recertifies them at each candidate influence cap; it verifies itself against the released E1 artifact before reporting. Package versions, the seeding rule, and the provenance block are detailed in Appendix A.3.

# 4 Results

We evaluate CertGate on a synthetic multi-site cohort whose every generative mechanism is known by construction. The point of a synthetic harness is not realism for its own sake but *oracle access*: because we control the data-generating process, we can compute the true answered-set risk at each target site and therefore score whether a certificate was earned or merely asserted. No real cohort can supply that ground truth, and a certificate only ever checked against the same unlabeled deployment data it was built on cannot be falsified. Because record-level selective certificates have been shown to overrun their budgets under grouped deployment [@zhou2026falsesense], the validation design must be able to catch the certificate failing; the seven experiments below therefore include a verified-falsifiable negative control and a record-as-unit comparator — the alternative this design exists to replace, run on the same draws — alongside the validity and capacity studies.

## 4.1 Experimental setup

**Generator.** The cohort follows the specification frozen in `data.py`: 208 collection sites, each contributing a heavy-tailed number of records drawn from a clipped lognormal (log-mean 6.0, log-sigma 1.1, clipped to $[20, 5000]$), for site sizes spanning roughly 20 to 5,000 records. Each record carries $d = 8$ features; the class signal lives on a single normalized direction supported on the first four coordinates, with the two class means separated by $\mathrm{sep} = 2.2$ along that direction, so features 0–3 are informative and features 4–7 are noise. The base outcome prevalence is 0.095. Site heterogeneity enters through a site random effect $u_c \sim \mathcal{N}(0, 0.5^2)$ that shifts each site's outcome log-odds, $\pi_c = \sigma(\mathrm{logit}(0.095) + u_c)$, so per-site prevalence varies around the 9.5% mark and no two sites share an identical case mix. This follows the distributional profile reported across multi-site clinical studies — many small-to-large sites with heavy-tailed sizes, single-digit prevalence, and site-level heterogeneity — while remaining fully known, in keeping with the cluster-as-unit reporting culture that motivates the design [@tripodcluster2023; @internalexternal2021].

**Outcome and time.** The generator emits one fully ascertained binary label per record and carries no time axis: there is no index event, no prediction time, no ascertainment window, and so no censoring, competing risk, or loss to follow-up. The clinical-target specification of Section 3.1 is therefore a requirement the method places on a real deployment, not something these experiments instantiate; what the harness supplies is the condition under which the certified error rate is well defined — a label observed for every record in every pool. Every error reported below is the symmetric 0–1 error of Section 3.1, formed at the head's $\hat{p} = 1/2$ decision boundary.

**Splits and model.** Sites are partitioned disjointly — 40% $S_{\text{train}}$, 20% $S_{\text{aux}}$, 40% $S_{\text{cal}}$ — with a hard site-disjointness assertion at pipeline entry. The risk head is an L2-regularized logistic regression ($C = 1.0$) fit on $S_{\text{train}}$; $S_{\text{aux}}$ orders the threshold walk and fits the label-shift machinery; $S_{\text{cal}}$ enters the certified path only through the certification walks (Section 3.2).

**Replication design.** Every experiment runs in mode FULL under protocol seed 20260721, over the $\alpha \in \{0.05, 0.10\}$ ladder at confidence $1-\delta$ with $\delta = 0.05$. The statistical experiments (E1–E4, E7, and E5's replication arm) replicate over $R = 200$ independent calibration draws, with the calibration draw — not the target site — as the unit of replication, because the certificate's $1-\delta$ event is over the draw of calibration sites; E5's case studies and E6 are single-draw exhibits of the explanation and composition artifacts. Identical inputs produce byte-identical certificates.

**Violation protocol.** Results are scored with the protocol of Section 3.9: aggregate conformance against the certified estimand (required to stay at or below $\delta$), and — without a $\delta$ target — the per-site dispersion diagnostic and the realized exceedance rate against its binomial reference. Because every rate below is a proportion over $R = 200$ independent draws, we accompany the primary rates with exact (Clopper–Pearson) 95% confidence intervals.

## 4.2 E1 — Validity in distribution

With target sites exchangeable with the calibration sites, the $\alpha = 0.10$ rung certifies every one of the 200 draws (certify rate 1.0) at mean answered-set coverage 0.9828, measured on each draw's target pool (minimum 0.871 across the 200 draws — no certificate here is bought by abstaining). Scored against the certified estimand — the influence-weighted answered risk $R_M$ on a fresh 200-site evaluation pool, one per draw — the certificate conforms on every draw: 0 of 200 certified draws exceed $\alpha$ (exact 95% CI $[0, 0.018]$, consistent with the rule-of-three bound $3/200 = 0.015$), against the $\delta = 0.05$ target, at a mean fresh-pool $R_M$ of 0.0567 — a little over half the budget, so the certificate is not sitting on its boundary.

The per-site dispersion diagnostic, which carries no $\delta$ target, records what the certificate declines to bound: the single-fresh-site hard-violation rate is 0.02 (4 of 200) and on average 5.5% of evaluation-pool sites exceed $\alpha$. A sensitivity arm separates the two quantities directly. Raising the site random effect $s_u$ from its protocol value of 0.5 to 1.0 and then 2.0 leaves aggregate conformance untouched — 0.0 exceedances in all three arms, at mean fresh-pool $R_M$ 0.0567, 0.0563, and 0.0519 — while the per-site diagnostic climbs: hard-violation rate 0.02, 0.10, 0.095, and mean per-site exceedance fraction 0.055, 0.1285, 0.1634. This is the aggregate-versus-individual signature that clause (2) of the guarantee names: heterogeneity leaves the certified average inside budget and moves the per-site tail. It is also why the $\delta$ target attaches to the aggregate and not to the diagnostic.

The realized exceedance rate is 0.08 overall, and Table 1 stratifies it by answered-set size against the binomial reference — the exceedance a valid certificate sitting exactly at the $\alpha$ boundary would show purely from label dispersion. Across every size bin the observed exceedance sits far below the reference (for example 0.0556 observed against 0.4838 expected in the 100–300 bin, and 0.0833 against 0.4898 in the largest bin), confirming that answered pools are not sitting at the $\alpha$ boundary but well inside it. The gap between the two curves is the parameter-versus-realized-count distinction of Section 3.9 in action.

**Operating characteristics.** A budget on an error rate can in principle be met by a degenerate rule, so we report what the answered and declined sets contain (Table 7). Pooled over the 200 fresh evaluation pools at $\alpha = 0.10$, the answered set holds 27,972,430 records at outcome prevalence 0.0968 and answered error 0.0567, with sensitivity 0.550, specificity 0.986, PPV 0.803 and NPV 0.953; $76.9\%$ of the answered errors are false negatives — the asymmetry the symmetric 0–1 loss of Section 3.1 does not price. The declined set holds 517,230 records, $1.8\%$ of the pooled total, at positive fraction 0.492 — roughly $4.7\times$ the pools' own prevalence of 0.1039 — so abstention concentrates on cases whose outcome is genuinely in doubt and routes $8.6\%$ of all positives to a clinician. Two readings follow. The gate is not meeting its budget by answering only easy negatives: the answered set's positive fraction is 0.0968 against the pools' own 0.1039, a depletion of 0.7 percentage points rather than the collapse a negative-skimming gate would show. And the $\alpha = 0.10$ rung is only marginally stricter than the trivial rule — a constant always-negative predictor errs at 0.1039 on these pools, just above $\alpha = 0.10$ and far above $\alpha = 0.05$ — so at this prevalence the error budget alone is a weak test, and the sensitivity and composition columns rather than the error rate are what show the gate doing work.

The certified quantity and the realized record-level error agree closely on these pools: the unweighted answered error averages 0.0566 across draws — 0.0567 when pooled over records, as in Table 7 — against $R_M$'s 0.0567, a gap of 0.0001. The influence cap is not masking a heavy-tailed site here — worth stating precisely because the cap exists for the case where it would, and E6 (Section 4.7) is a pool where the two do separate.

The stricter $\alpha = 0.05$ rung certifies nothing at 208 sites (certify rate 0.0): the system declines rather than issue a certificate it cannot support at this cluster count. That decline, not a violation, is the correct behavior, and E4 maps exactly how many sites $\alpha = 0.05$ would require.

## 4.3 E2 — Label shift

We shift site-level prevalence from the source value of 0.095 up to a target base rate of 0.22 — a 2.3-fold increase in outcome frequency of the kind that a change in referral pattern or case mix produces — and compare the exchangeable baseline against the BBSE label-shift mode [@lipton2018bbse].

The uncorrected baseline is the motivating failure: at $\alpha = 0.10$ it certifies all 200 draws and hard-violates 39.5% of them (hard-violation rate 0.395, exact 95% CI $[0.327, 0.466]$; exceedance 0.585; decline rate 0.0), having assumed away the shift that occurred. The interval lies far above $\delta = 0.05$ — and the conservative per-site criterion understates the damage: scored against the certified estimand itself, the influence-weighted answered risk on a fresh label-shifted 200-site pool exceeds $\alpha$ for 97.5% of the baseline's certificates. Under this shift the baseline certificate is not occasionally wrong; it is almost always wrong about the very quantity it certifies.

BBSE issues no certificate at all on this configuration. It declines all 200 draws at both rungs: 194 because no threshold in the walk certified at both $\rho$ endpoints (failsafe), and 6 because the $q$ interval fell outside the box's admissible range, so the implied prevalence left $(0,1)$ (misspecified). The joint event the protocol asks about — a certificate issued *and* hard-violating — is therefore 0 of 200 trivially (exact 95% CI $[0, 0.018]$), and the baseline mode declines entirely at $\alpha = 0.05$ as well. This is a weaker claim than a certified-and-clean result would have been, and it is the honest one: once the target predicted-positive rate carries its own confidence share, a single-site target pool of this size does not supply enough information to support a certified rung under this shift, and the mode refuses rather than repeat the baseline's 39.5% overclaim. Declining is the designed behavior (Section 3.6), not a fallback. Whether BBSE certifies on larger or multi-site target pools is a question about pool size that this configuration does not answer.

A magnitude sweep separates a working correction from a mode that merely always declines. Holding the protocol fixed and moving the target base rate through $\{0.095, 0.13, 0.16, 0.19, 0.22\}$ ($R = 100$ per non-anchor point, on independent seed streams): the baseline's failure is dose-responsive — its hard-violation rate climbs 0.0, 0.07, 0.08, 0.21, 0.395, and its aggregate-estimand violation rate 0.0, 0.0, 0.0, 0.17, 0.975 — while BBSE's certify-and-violate rate is 0.0 at *every* magnitude. And at the null point (no shift at all), BBSE is not a reflexive decliner: it certifies 9% of draws with zero violations, declining the rest for want of margin. The mode certifies when the pool supports it and refuses in proportion to what the shift destroys, which is the behavior "certifies-or-declines" claims.

## 4.4 E3 — Concept-shift negative control

A negative control that cannot fail proves nothing. Concept shift is the failure mode the guarantee explicitly excludes (Section 3.6), and E3 demonstrates that the exclusion is real rather than rhetorical. We inject a posterior tilt (concept intercept 2.0) and the harness first *verifies the poison*: it confirms that the tilt raises the mean realized answered error to 0.161, above the $\alpha = 0.10$ budget (`tilt_pushes_risk_above_alpha` true). This check is enforced, not decorative — a tilt that failed to raise that error above $\alpha$ aborts the run before any output is written (reason `e3-control-not-poisonous`), so the control cannot silently degrade into a test the certificate is guaranteed to pass.

Given a genuinely poisonous shift, the $\alpha = 0.10$ certificate certifies all 200 draws and hard-violates 70.0% of them (hard-violation rate 0.700, exact 95% CI $[0.631, 0.763]$; exceedance 0.845) — and against the certified estimand itself the failure is total: the influence-weighted answered risk on a fresh concept-tilted 200-site pool exceeds $\alpha$ for 100% of the certificates issued. This is the intended result: the assumption tag is load-bearing, and a certificate that kept passing under verified concept shift would be the alarming outcome. Demonstrating the failure openly is validation rigor — the constructive counterpart to the published warning that selective certificates can mislead when their assumptions are not checked [@zhou2026falsesense].

## 4.5 E4 — Cluster-count sweep

E4 is the capacity-planning study: it sweeps the site count over $\{60, 100, 150, 208, 300, 400\}$ at both rungs and reports what budget each scale can support (Table 4).

At $\alpha = 0.10$ the certify rates are $[0.0, 0.0, 1.0, 1.0, 1.0, 1.0]$: certification switches on at 150 sites and holds from there, with mean coverage 0.9372 at 150, 0.9818 at the realistic 208-site scale, 0.9754 at 300, and 0.9641 at 400. (The 208-site sweep point is a separate run from E1, with independently derived seeds — hence 0.9818 here against E1's 0.9828.) The 60- and 100-site points are declined, and the reason is specific: the minimum-cluster gate counts only record-carrying sites and requires 50 of them, so any run with fewer than roughly 125 sites yields a ~40% calibration share below 50 clusters and is refused by that gate — not by the betting test's information floor. We say so explicitly because the two failure modes have different remedies.

At $\alpha = 0.05$ the certify rates are $[0.0, 0.0, 0.0, 0.0, 0.285, 1.0]$: the stricter budget is unreachable through 208 sites, first appears at 300 (certify rate 0.285, coverage 0.7296), and becomes reliable only at 400 (certify rate 1.0, coverage 0.8516). The frontier is set by the information floor scaling as $1/n_{\text{cal}}$, and it makes the operating regime concrete: at the 208-site scale $\alpha = 0.10$ is the operative rung, and a stricter answered-set budget is a claim about how many sites one has, not about the method.

## 4.6 E5 — Explainability case studies

Because the head is linear, the answer/abstain decision comes with exact additive attributions $\phi_j$ — genuine Shapley values, not sampled approximations [@lundberg2017shap] — attached directly to the certified gate [@artelt2022reject; @ifac2025abstainexplain]. This case study uses a deployment with threshold $\tau^* = 0.55$, answering 200 cases and declining 2. The global standardized importances recover the generator: features 0–3 dominate at 1.157, 1.161, 1.178, and 1.155, while features 4–7 are negligible ($|\cdot| \le 0.036$).

Three representative cases illustrate the abstention explanation, which reports the margin-to-answer $m(x) = \mathrm{logit}\,\tau^* - |\mathrm{logit}\,\hat{p}(x)|$. A confidently answered case (index 48, score 0.99997) clears the answer bar by more than 10 logits — deep inside the answer region. A declined case (index 38, score 0.5262) sits just short, with a positive margin-to-answer of 0.0956 logit units; a near-threshold declined case (index 102, score 0.5445) is even closer, margin 0.0223. In each declined case the signed attributions localize the confidence deficit to specific features, so the abstention reads as "declined because the informative features leave confidence below the certified bar" rather than an opaque refusal.

At the cohort level, the answered and declined attribution profiles of this single deployment differ most on feature 0: its mean absolute attribution is 0.868 across the 200 answered cases against 1.722 across the 2 declined ones (gap $-0.854$). With $n_{\text{declined}} = 2$, that describes these two abstentions, not a cohort-level driver — so we replicated the profile over $R = 200$ independent calibration draws, each scored at its own deployed threshold (186 draws produced declines; 2,644 declined cases pooled; mean decline rate 2.2%). The single-draw reading does not survive: the feature with the largest answered-to-declined gap is essentially uniform across the four informative features (top-feature counts 47, 40, 51, 48 for features 0–3; modal share 0.274 against a 0.25 chance level), so no stable single driver exists — exactly what the generator's symmetric construction predicts, since features 0–3 carry equal loadings on one shared signal direction. What does replicate is the *shape* of the profile: declined cases carry systematically larger attribution magnitudes on every informative feature (mean gaps $-0.26$, $-0.24$, $-0.23$, $-0.21$ with 95% CIs of roughly $\pm 0.05$–$0.07$), the signature of contested cases whose large pulls cancel rather than of quiet cases with nothing to say. We report the null as a null: the cohort-level instrument works, and on this generator it correctly finds no privileged feature.

## 4.7 E6 — Per-site coverage uniformity and composition

E6 uses a separate single-draw deployment (threshold $\tau^* = 0.55$; 40 target sites) to ask whether coverage is uniform across site sizes and how the gate earns its low answered error. The equity question here is scoped narrowly to site size — whether small hospitals receive systematically worse selective service than large ones; demographic and protected-attribute subgroup analysis is beyond this synthetic harness. Table 2 stratifies per-site coverage and answered error by site size. Coverage is essentially flat across bins — 0.9896, 0.9800, and 0.9856 for the [30,100), [100,300), and [300,$\infty$) bins respectively — with no size-based coverage collapse (range 0.980–0.990; the smallest populated bin holds 4 sites), and the mean answered error stays below $\alpha = 0.10$ in every bin (0.0523, 0.0670, 0.0595). Small sites are neither over-answered nor starved, and no site answers nothing at all (0 of 40 at zero coverage), so the record-carrying-but-silent case that would enter certification as a neutral atom (Section 3.3) is not exercised here either.

The pool-level operating characteristics complete the picture (Table 7). At $\tau^* = 0.55$, deployed at $\alpha = 0.10$ under the exchangeable tag, the gate answers 25,314 of the pool's 25,682 records and declines 368. On the answered set, sensitivity is 0.541, specificity 0.985, PPV 0.785 and NPV 0.955, with $75.6\%$ of answered errors false negatives; the 368 declined records carry a positive fraction of 0.462 against the pool's own 0.0968. Here, unlike E1, the certified quantity and the record-level average separate: $R_M$ on this pool is 0.0619 against an unweighted answered error of 0.0555, a 0.6-percentage-point gap in the conservative direction. The cap is the reason — every site with at least 100 records enters with the same influence, so the pool's 15 mid-sized sites, which run an answered error of 0.0670, count as heavily as its 21 largest at 0.0595. That is the influence weighting doing what it exists for, and it is visible only because both numbers are reported.

The composition analysis (Table 3) is the instrument that tells us *how* a 9.5%-prevalence task yields a sub-$\alpha$ answered error. Over the 25,314 answered records, the predicted-class positive fraction is 0.0630 (1,596 records, tagged estimated), the BBSE-implied true-class fraction is 0.0858 (expected 2,170.9 positives, $\hat{\rho} = 0.830$, tagged estimated under the label-shift assumption), and the oracle true-class fraction is 0.0915 (2,316 records, harness-only diagnostic). The three estimates agree in order, and the reading they support is a specific one: at this operating point the gate answers nearly every record (per-site coverage 0.98–0.99), and the answered set's oracle positive fraction, 0.0915, sits just under the $\sim$9.5% cohort prevalence rather than well below it — so on this deployment the sub-$\alpha$ answered error is not bought by answering only easy negatives. The predicted-class column alone would have suggested otherwise, understating the true positive load by about a third; without the oracle column one could not tell which reading was right, which is precisely why the three-way report exists. The roughly 0.6-percentage-point gap between the BBSE-implied and oracle fractions is the visible cost of the label-shift correction's estimation step. A gate run at a more selective threshold could still earn its budget by concentrating on easy negatives; this instrument is what would show it.

## 4.8 E7 — Record-as-unit comparator

The design's premise — that treating records as the unit of independence produces overconfident certificates on site-clustered data — entered the paper as a citation [@zhou2026falsesense]; E7 demonstrates it in this harness, on the same draws the site-unit walk certifies. The comparator is the natural record-level certifier in the reject-option lineage: the identical betting machinery with each *record* as one atom ($g_i = 1$, no influence cap), walked over the same threshold grid with its order learned on an equal-sized $S_{\text{aux}}$ record sample, run on a 2,000-record subsample of $S_{\text{cal}}$ — still 25 times the site count, and a floor 25 times smaller. Both certifiers are scored identically, against the influence-weighted answered risk of one shared fresh 200-site pool at their own deployed thresholds, over $R = 200$ draws at two heterogeneity levels.

At $\alpha = 0.05$ — the rung the site-unit walk refuses on every one of 400 draws across both arms — the record-unit certifier *grants* the budget almost always: certify rate 1.0 at $s_u = 0.5$ and 0.99 at $s_u = 2.0$. Those grants are not free. At the protocol heterogeneity its certificates violate the aggregate budget in 3.5% of draws; at $s_u = 2.0$, the heterogeneity regime E1's sensitivity arm anticipates, the violation rate is 9.6% — nearly twice $\delta$ — for a certificate whose stated confidence is unchanged. At $\alpha = 0.10$ both units certify essentially always and neither violates, but the record-unit walk deploys materially laxer thresholds under heterogeneity (mean $\tau$ 0.587 against the site-unit's 0.747 at $s_u = 2.0$), running the deployment at a mean fresh-pool risk of 0.081 against the site-unit's 0.052 — most of the way to the boundary, with no budget left for the cluster variance its independence assumption ignores. The comparison is the necessity argument for contribution 1 in one table: the record-as-unit certifier buys its extra rungs and coverage by spending confidence the clustered data does not have, and the site-as-unit walk's refusals are where that spending would have happened.

## 4.9 Sensitivity to the frozen influence cap

Section 3.2 pins the design constants, and pinning is not justification. Of the constants in Table 5, $M$ is the one whose value visibly moves the certificate, so we sweep it: E1's exchangeable arm ($s_u = 0.5$, $R = 200$ draws) is re-run in the baseline mode — the mode that deploys on 194 of those 200 draws — at $M \in \{25, 50, 100, 200, 500, 1000, 5000\}$, re-deriving both the $S_{\text{aux}}$ walk order and the calibration walk at each value, and rescoring each resulting certificate against $R_M$ *at that same $M$* on the same fresh 200-site evaluation pool. Each $M$ certifies its own estimand, so the table compares procedures rather than tracking one quantity. As a check on the replay, at $M = 100$ it reproduces all 194 recorded baseline thresholds exactly.

Table 6 shows a plateau and then a cliff. Across $M \in \{25, 50, 100\}$ the procedure is effectively invariant — certify rate 1.0, mean $\tau^*$ 0.552–0.554, mean coverage 0.984, mean fresh-pool $R_M$ 0.057, zero exceedances of the certified estimand — because at all three values most records already sit above their own site's cap ($96\%$, $93\%$, $86\%$) and $R_M$ is close to a site-equal average either way. Behavior begins to move at $M = 200$ (mean $\tau^*$ 0.566, mean coverage 0.980) and changes materially above it: by $M = 500$ the walk buys its certificate with coverage (mean $\tau^*$ 0.688, mean coverage 0.939), by $M = 1000$ the certify rate falls to 0.03, and at $M = 5000$ — the generator's upper size clip, where $g_c = n_c$ for every site and the estimand becomes the record-proportional answered error — nothing certifies at 208 sites. The stricter $\alpha = 0.05$ rung is unreachable at every $M$ in the grid, so the frontier of Section 4.5 is not an artifact of this constant. (Coverage in Table 6 is measured on the 200-site evaluation pool, which is why its $M = 100$ row reads 0.9838 against Section 4.2's target-pool 0.9828: the pools differ, not the certificate.)

Two things follow for Section 3.2's discipline. The sensitivity runs in the safe direction: every $M$ above the frozen value certifies *less*, and the values that certify identically are smaller, so $M = 100$ is not a setting that makes certification easy. And the choice is consequential in a way tuning language does not capture — $M$ selects a point on a continuum between a site-equal and a record-proportional estimand, and at this cluster count only the site-equal end is certifiable at all. That is the same fact E7 reports from the other direction.

# 5 Discussion

## 5.1 What the certificate does and does not promise

CertGate issues a specific, bounded promise, and its value depends on reading it precisely: a bound on the influence-weighted answered-set error *parameter* $R_M$ **averaged over the population of sites from which the calibration sites were drawn**, holding with probability at least $1-\delta$ over that draw, under one explicitly tagged assumption; in the exchangeable mode the certified thresholds are a function of the calibration draw alone, so every target pool the certificate is applied to shares that one $1-\delta$ event. It is not a per-site bound. Under between-site heterogeneity an individual site can exceed $\alpha$ while the average holds, at a rate the certificate does not measure, and E1's $s_u$ arm shows exactly that: aggregate conformance stays at zero exceedances while the per-site diagnostic climbs from 0.02 to 0.10 as $s_u$ rises from 0.5 to 2.0 (Section 4.2). Nor is it a bound on a batch's realized error *count* — E1's realized exceedance runs at 0.0833 against a 0.4898 binomial reference at the $\alpha$ boundary in the largest size bin — nor a per-record coverage statement, nor protection against concept shift. The clearest illustration of what the tag buys is where it breaks: E3's verified tilt (mean realized answered error 0.161, above $\alpha$) produces certificates that hard-violate 70% of the time, consistent with a certificate whose assumption no longer holds (Section 4.4). The negative control is validation design, not a headline contribution.

## 5.2 Site count, not record count, is the capacity constraint

Treating the site, not the record, as the unit of independence reframes what capacity means for a certified gate. Statistical power comes from calibration *sites* — roughly 80 of them at the 208-site scale — not from the $\sim\!10^5$ records they contain. E4 maps this frontier directly: $\alpha=0.10$ becomes reachable around 150 sites, while $\alpha=0.05$ first appears at 300 and is reliable only by 400 (Table 4). For any reader tempted to call $\alpha=0.10$ a weak guarantee: the operative rung is a property of the available cluster count, not of the method — buying $\alpha=0.05$ costs sites, and E4 prices that purchase.

## 5.3 A model-agnostic gate with a linear head

The gate remains model-agnostic (Section 3.8): the score enters only through the ranking it induces, and we deploy a linear head only for the attribution layer. A black-box head can be swapped in; the gate would then price its selective quality visibly, as a change in certified coverage, rather than letting a better-looking model smuggle in an unearned guarantee.

## 5.4 Relevance to cross-institutional deployment

Section 1's motivating case — a score calibrated at a handful of academic centers, then deployed at a community hospital that contributed no training records — maps onto the two assumption tags directly, and that mapping is the practical content of the guarantee. If the community hospital differs from the calibration sites only in ways already represented in the between-site variation calibration sampled, the exchangeable tag applies and the certificate holds as a site-population average — subject to clause (2), since that one hospital may sit in the tail the average does not bound. If it differs mainly in outcome prevalence, through a different referral pattern or a different case mix at the same presentation, the label-shift tag applies and E2 is the relevant evidence: at the pool sizes tested there, that mode declines rather than certifies. If it differs in how features relate to outcome — different documentation practice, different treatment thresholds — neither tag applies, the shift is concept shift, and E3 shows what the certificate does then. For a deployer the summary is that the first case is the one CertGate currently certifies, the second is one it recognizes and refuses at these pool sizes, and the third is out of scope and undetectable without labels.

Because the guarantees are cluster-level, the machinery is relevant to cross-institutional deployment, where each institution is a natural cluster. We state this as relevance, not as a federated method: CertGate assumes site-disjoint calibration data are available centrally and does not address the communication, privacy, or aggregation mechanics of federation. Federated risk-control work such as [@fedcrc2026] treats sites as first-class but through in-expectation risk-curve shrinkage; a certified gate under genuine federation constraints is future work.

## 5.5 Toward real-data deployment

The synthetic-first posture is a deliberate validation choice, not a deferral. The generator reproduces the distributional features of multi-site clinical data — 208 sites, lognormal sizes 20–5,000, prevalence $\sim\!9.5\%$, site random effects — but every mechanism is exact by construction, so oracle labels and the true answered risk are available to check the certificate against. Real data cannot supply that ground truth, which is what makes it unable to validate a validity claim. The route to real data is concrete: the implementation includes a `from_raw` loader and a worked example that carries a cohort through the same site-disjoint pipeline. Applying CertGate to a real multi-site cohort is ongoing work.

# 6 Conclusion

CertGate occupies the intersection of three separately developed lines: certified selective risk [@chow1970reject; @geifman2017selective], cluster-as-unit distribution-free inference [@dunn2023hierarchical; @lee2025hierarchical], and label-shift guarantees that propagate weight uncertainty into the guarantee itself [@si2024pac]. Assembling them is not automatic: the influence-weighted estimand, the cluster-robust BBSE box, and decline behavior at practical site scales are obstacles that appear only in combination, and the E4 frontier prices that difficulty in units of sites. Its posture throughout is disclosure: the guarantee is scoped to a tagged assumption; the certified quantity is named as a site-population average, with the between-site dispersion it does not bound reported beside it; the answered-set parameter is kept distinct from any realized batch count; and the concept-shift boundary is demonstrated, not asserted. The same site-disjoint pipeline extends to real multi-site data; applying it to a documented clinical cohort is the immediate next step.

## 6.1 Limitations and future directions

We state the boundaries of the current method plainly; each is a stated limitation rather than a solved problem, and none is patched over in the results.

*The guarantee is a site-population average.* Individual sites are not bounded. Per-site dispersion under heterogeneity is measured (E1's $s_u$ arm, Section 4.2) but not controlled, and a genuine per-site guarantee would need a tolerance bound on an upper quantile of the per-site risk distribution — a different test, costing substantially more clusters than the ladder certifies here.

*Concept shift is undetectable from unlabeled data.* No method that observes only target features can distinguish a benign covariate or label shift from a change in $P(y\mid x)$. E3 is a direct demonstration of this boundary, and the certificate carries the concept-shift caveat in its guarantee text rather than in a footnote.

*Covariate-shift importance weighting is deliberately excluded.* A third assumption mode that reweights by feature-density ratios is not offered because, at practical clip caps, the effective-sample-size loss structurally prevents certification below roughly 400 clusters — the clip cap divides the certification margin under the information floor — outside the operating range this cohort supports. We ship the two modes the data can actually certify rather than a third that would decline everywhere.

*There is no out-of-support screen.* The confidence gate and the BBSE correction carry what robustness the method has against target cases far from the calibration support; a dedicated nearest-neighbor (kNN) out-of-support screen is future work, not a component we claim now.

*Temporal common-shock correlation is not modeled.* Sites sharing the collection window may be correlated through common temporal shocks — practice changes, seasonal case-mix. The site-as-unit analysis treats sites as the independent unit and does not model residual cross-site dependence induced by shared time.

*Missingness is the caller's responsibility, and there is no positivity diagnostic.* The pipeline validates features at entry and rejects non-finite values loudly rather than imputing them, so any missing-data handling happens upstream in the caller's ETL and is outside what the certificate speaks to; the certificate cannot see, and does not account for, whatever that upstream step did. We also add no dedicated positivity (overlap) diagnostic, so structurally unsupported regions of feature space are not flagged as such.

*The loss is symmetric and the operating point is inherited.* $\text{err}_i$ counts a false negative and a false positive alike, so $\alpha$ budgets their sum and not their clinical cost — and at this prevalence roughly three quarters of answered errors are false negatives (Sections 4.2 and 4.7). The certificate also inherits the head's $\hat{p} = 1/2$ decision boundary and says nothing about whether that boundary suits a given clinical decision. A cost-weighted or net-benefit criterion would need a different atom: the linearization of Section 3.3 carries through for any bounded per-record loss, but the estimand, the guarantee text and every result here are the symmetric 0–1 case.

*Only one frozen constant is swept.* Section 4.9 varies the influence cap $M$ because it is the constant that moves the certificate. The split fractions, the threshold grid's resolution, the two BBSE decline thresholds and the bootstrap counts are pinned with a stated basis (Table 5) but not varied. What the pinning test attests is that their values were fixed before any result was read; it attests nothing about whether they are the best values.

*Three of the four BBSE intervals are asymptotic, and measurably so.* The percentile cluster bootstrap that propagates $(c_0, c_1, \pi_s)$ uncertainty into $[\rho_{lo}, \rho_{hi}]$ is asymptotic, and its finite-cluster error is material at this design's scale: with the head held fixed we measure the box's realized joint miscoverage at 0.038 against a nominal $\le 0.019$. The fourth interval, on $q_{\text{target}}$, is finite-sample (Clopper–Pearson) for a single-site pool and a cluster bootstrap otherwise, with $\delta_{\mathrm{conf}}=0.025$ spent across the four-parameter box and $\delta_{\mathrm{bet}}=0.025$ on the betting test. This is disclosed wherever the guarantee is stated; replacing the percentile box with a finite-sample cluster bound is future work.

# Appendix A: Deferred proofs

## A.1 Validity of the betting test

We show the crossing rule $\sup_t K_t \ge 1/\delta$ (Section 3.4) is a finite-sample level-$\delta$ test of $H_0: \mathbb{E}[Z] \ge \alpha$.

*(i) Boundedness.* Each term of the inner sum in $Z_c$ lies in $[-\alpha, 1-\alpha]$, so the prefactor $g_c/(M n_c)$ scales the sum into $[-\alpha\, g_c/M,\ (1-\alpha)\, g_c/M] \subseteq [-\alpha, 1-\alpha]$ because $g_c \le M$; adding $\alpha$ places $Z_c \in [0,1]$.

*(ii) Null equivalence.* Both the atom mean and the estimand are taken over the same index set: $c$ ranges over the population of sites from which the calibration sites are drawn, so $R_M = \mathbb{E}[g_c a_c e_c] / \mathbb{E}[g_c a_c]$ is a site-population parameter and $\mathbb{E}[Z]$ is an expectation over that same site draw. By the identity $Z_c - \alpha = g_c a_c (e_c - \alpha)/M$ of Section 3.3, $\mathbb{E}[Z] - \alpha = \mathbb{E}[g_c a_c (e_c - \alpha)]/M$, whose sign is the sign of $\mathbb{E}[g_c a_c e_c] - \alpha\,\mathbb{E}[g_c a_c]$ and hence of $R_M - \alpha$. Testing $\mathbb{E}[Z] \ge \alpha$ is therefore exactly testing $R_M \ge \alpha$ — for the population average, and for no individual site: for a single site $g_c$ and $a_c$ cancel and the quantity collapses to that site's own error rate, which a population mean does not bound (Section 3.7, clause 2).

*(iii) Supermartingale.* The bet $\lambda_t$ is predictable — a function only of atoms already processed — and its cap at $0.9/(1-\alpha)$ keeps every factor $1 + \lambda_t(\alpha - Z_t) \ge 0.1 > 0$; under $H_0$, $\mathbb{E}[\,1 + \lambda_t(\alpha - Z_t) \mid \mathcal{F}_{t-1}\,] \le 1$, so $K_t$ is a nonnegative supermartingale with $K_0 = 1$.

*(iv) Ville.* For such a process, $P(\sup_t K_t \ge 1/\delta) \le \delta$ [@ville1939], so the crossing rule has finite-sample level $\delta$ — no asymptotics, and no distributional assumption beyond the boundedness in (i). The test's boundary behavior (type-I error at $\mathbb{E}[Z] = \alpha$) is additionally pinned by the unit test suite.

## A.2 Soundness of the dual-endpoint BBSE certification

The uncertainty box (Section 3.6) is a Bonferroni box over four estimated parameters $(c_0, c_1, \pi_{\text{source}}, q_{\text{target}})$, each interval taken at level $\delta_{\text{conf}}/4$. The first three come from a cluster bootstrap of the $S_{\text{aux}}$ sites that requires 2,000 valid resamples within at most 4,000 attempts (a degenerate pool triggers a decline); the fourth is an exact two-sided Clopper–Pearson interval on the target pool's predicted-positive count for a single-site pool, and a cluster bootstrap over target sites for a pool declaring ten or more (a pool declaring two to nine declines). The box is propagated to $[\rho_{\text{lo}}, \rho_{\text{hi}}]$ by *corner-interval coverage*: the clipped $\rho(c_0, c_1, \pi_s; q)$ is coordinate-wise monotone in each of the four box parameters on the gated region, the minimum and maximum of $\rho$ over the sixteen box corners bound every interior parameter combination, and the clip on $\pi_t$ only widens the resulting interval, so the corners bound every $\rho$ the box implies. Certification therefore reduces to the two endpoints, which is sound for the following reason.

The certified statistic is scale-invariant in the record weights, so the sign that decides certification factors as $\operatorname{sign}(\mathbb{E}[Z(\rho)] - \alpha) = \operatorname{sign}(A + \rho B)$ with $(A, B)$ free of $\rho$ — affine in $\rho$. The certifiable set $\{\rho : \mathbb{E}[Z(\rho)] \le \alpha\}$ is therefore convex, so certifying at both endpoints of $[\rho_{\text{lo}}, \rho_{\text{hi}}]$ covers every interior $\rho$, and any $\rho$ inside the box with $\mathbb{E}[Z(\rho)] > \alpha$ forces one endpoint whose level-$\delta_{\text{bet}}$ test bounds the false-certification probability. This holds even though the two endpoint atom sets use different per-endpoint normalizations $w_{\max} = \max(1, \rho)$, under which the *raw* atom mean is piecewise with a kink at $\rho = 1$: the affine object is the sign-determining product $(\mathbb{E}[Z]-\alpha)\cdot\max(1,\rho)$, not the kinked atom mean itself. The argument is pinned numerically in the test suite on the deployed normalization, including an interval that straddles $\rho = 1$.

## A.3 Software and reproducibility details

Every result regenerates from the released code under a pinned environment (`requirements.txt`): Python 3.13.3 with `numpy==2.5.0`, `scipy==1.18.0`, `scikit-learn==1.9.0`, `joblib==1.5.3`, `threadpoolctl==3.6.0`, `matplotlib==3.11.0`, and `pytest==9.1.1`; no undeclared dependencies are used, and all third-party imports sit at module top level. All randomness derives from a single protocol seed (20260721): every per-experiment, per-$\alpha$, per-mode, and per-site seed is produced by a fixed SHA-256 rule from that one seed, so the whole grid is a deterministic function of it, and identical inputs produce byte-identical certificates. The frozen design constants — split fractions, budget ladder, influence cap $M$, threshold grid, betting-test parameters, and decline thresholds — are pinned to their literal values by a unit test, so any drift fails continuous integration. The full grid runs from a single command (`python -m experiments.run_synthetic`), which regenerates Figures 1–7 and Tables 1–4; each report artifact embeds a provenance block recording package versions, seeds, and input hashes. The influence-cap sweep and the operating-characteristic counts of Tables 6 and 7 are produced by a second read-only command (`python -m experiments.panel_s2_tables`) that reseeds the same E1 and E6 draws from the same protocol seed and recertifies them at each candidate $M$; it asserts against the released E1 artifact that its replay reproduces every baseline-deploying draw's certified threshold exactly (194 of 194, no mismatches) before reporting anything. The test suite is green at 136 passing tests, with one full-scale fixture arm skipped unless explicitly enabled. The anti-conservativity of naive realized-contribution truncation (Section 3.3) is pinned by a dedicated regression test, in which a construction with 17.5% true risk certifies at $\alpha = 5\%$ under truncation but is correctly refused under the influence-weighting scheme.

# Acknowledgements

[TO BE COMPLETED — optional]

# Data availability

All data used in this study are synthetic and generated deterministically by the included code, publicly available at [CODE REPOSITORY URL — to be added]. The experiment grid regenerates every figure and table reported here from a single command (`python -m experiments.run_synthetic`); no external or third-party datasets are used.

# Funding

[TO BE COMPLETED]

# Author contributions

[TO BE COMPLETED]

# Ethics approval and consent to participate

Not applicable: the study involves no human subjects and no patient data; all data are synthetic.

# Consent for publication

Not applicable.

# Competing interests

[TO BE COMPLETED]

# Figures

**Figure 1. E1 in-distribution validity.** Left: at $\alpha = 0.10$ the certified quantity — the influence-weighted answered risk on a fresh 200-site evaluation pool — exceeds $\alpha$ on 0 of 200 calibration draws, below the $\delta = 0.05$ dashed line, with the per-site dispersion diagnostic (0.02, no $\delta$ target) shown beside it; the $\alpha = 0.05$ rung issues no certificates at 208 sites. Right: realized exceedance by answered-set size bin (observed) against the binomial reference at the $\alpha$ boundary; the observed curve stays far below the reference in every bin (e.g. 0.0556 vs 0.4838 for [100,300)), showing the certificate binds the risk parameter rather than any batch's realized count.

**Figure 2. E2 label shift: anchor failure and magnitude sweep.** Left: at the anchor shift (0.095 $\to$ 0.22) the uncorrected exchangeable baseline (red) hard-violates 39.5% of its $\alpha = 0.10$ certificates, far above the $\delta = 0.05$ dashed line; the BBSE bar (blue) is empty because BBSE declines all 200 draws (194 failsafe, 6 misspecified), so certify-and-violate is 0 of 200 trivially, and neither mode certifies at $\alpha = 0.05$. Right: sweeping the target base rate through $\{0.095, 0.13, 0.16, 0.19, 0.22\}$ at $\alpha = 0.10$ — the baseline's hard-violation rate is dose-responsive (0.0 to 0.395) while BBSE's certify-and-violate rate stays at 0.0 at every magnitude; at the null point BBSE certifies 9% of draws with no violations, so the mode is a correction, not a reflexive decliner.

**Figure 3. E3 concept-shift negative control.** Under a posterior tilt verified to push the mean realized answered error to 0.161, above the $\alpha = 0.10$ budget, the certificate hard-violates 70% of draws, far above the $\delta = 0.05$ dashed line. The certificate is *supposed* to fail here — concept shift is out of scope for any unlabeled-data method — so this demonstrates that the tagged assumption genuinely constrains the guarantee rather than exposing a defect.

**Figure 4. E4 cluster-count sweep.** Left: certify rate versus site count for both rungs; $\alpha = 0.10$ (orange) switches on at 150 sites and holds at 1.0, while $\alpha = 0.05$ (blue) first appears at 300 (0.285) and reaches 1.0 only at 400. The shaded region marks sites declined by the 50-record-carrying-cluster gate (below ~125 sites), a different failure mode from the information floor. Right: mean answered-set coverage over the same sweep, reaching 0.9818 for $\alpha = 0.10$ at the realistic 208-site scale.

**Figure 5. E5 explainability.** Left: global standardized coefficients from the single-deployment case study recover the generator — features 0–3 dominate (~1.15–1.18) and features 4–7 are negligible ($|\cdot| \le 0.036$). Right: the answered-minus-declined gap in mean absolute attribution per feature, replicated over $R = 200$ draws (mean $\pm$ 95% CI across the 186 draws with declines; 2,644 declined cases pooled). Every informative feature shows a negative gap of similar size ($-0.21$ to $-0.26$) and the top-gap feature is uniform across features 0–3 (modal share 0.274 vs 0.25 chance), so declined cases are contested on the informative features generally, with no stable single driver — the expected null for this generator's symmetric loadings.

**Figure 6. E6 per-site coverage and answered error.** Mean answered error by site-size bin at $\tau^* = 0.55$: 0.0523, 0.0670, and 0.0595 for the [30,100), [100,300), and [300,$\infty$) bins, every bar below the $\alpha = 0.10$ dashed line. Per-site coverage (Table 2) is flat across bins (0.980–0.990), showing no size-based coverage collapse.

**Figure 7. E7 record-as-unit comparator ($R = 200$ per arm).** For each heterogeneity arm ($s_u \in \{0.5, 2.0\}$) and rung: certify rate (light) and aggregate-budget violation rate among certificates (solid) for the site-unit walk (blue) and the record-as-unit certifier (red), against the $\delta = 0.05$ dashed line. At $\alpha = 0.05$ the record unit certifies 99–100% of the draws the site unit refuses entirely, and violates its aggregate budget in 3.5% (s_u = 0.5) and 9.6% (s_u = 2.0) of them — the latter nearly twice $\delta$; at $\alpha = 0.10$ both certify, but the record unit deploys laxer thresholds and runs at nearly twice the fresh-pool risk under heterogeneity.

# Tables

**Table 1. E1 realized exceedance by answered-set size bin ($\alpha = 0.10$), observed versus binomial reference.**

| Answered-set size bin | Pools ($n$) | Observed exceedance | Binomial reference |
|---|---|---|---|
| [0, 30) | 2 | 0.0000 | 0.3937 |
| [30, 100) | 24 | 0.1250 | 0.4822 |
| [100, 300) | 54 | 0.0556 | 0.4838 |
| [300, $\infty$) | 120 | 0.0833 | 0.4898 |

**Table 2. E6 per-site coverage and answered error by site-size bin ($\tau^* = 0.55$, 40 target sites, single draw).** Answered error stays below $\alpha = 0.10$ in every bin; coverage does not collapse with size.

| Site-size bin | Sites | Mean per-site coverage | Mean answered error |
|---|---|---|---|
| [0, 30) | 0 | — | — |
| [30, 100) | 4 | 0.9896 | 0.0523 |
| [100, 300) | 15 | 0.9800 | 0.0670 |
| [300, $\infty$) | 21 | 0.9856 | 0.0595 |

**Table 3. E6 three-way answered-set composition ($n_{\text{answered}} = 25{,}314$).** At this near-full-coverage operating point the oracle positive fraction of the answered set sits just under the ~9.5% cohort prevalence; the predicted-class column understates it by about a third.

| Estimator | Positive fraction | Count | Tag |
|---|---|---|---|
| Predicted-class | 0.0630 | 1,596 / 25,314 | estimated |
| BBSE-implied true-class | 0.0858 | 2,170.9 expected ($\hat{\rho} = 0.830$) | estimated, label-shift assumption |
| Oracle true-class | 0.0915 | 2,316 / 25,314 | diagnostic, harness only |

**Table 4. E4 cluster-count sweep grid ($R = 200$ per point).** Certify rate and mean answered-set coverage at certifying points; "—" where nothing certifies.

| $n_{\text{sites}}$ | $\alpha=0.10$ certify | $\alpha=0.10$ coverage | $\alpha=0.05$ certify | $\alpha=0.05$ coverage |
|---|---|---|---|---|
| 60 | 0.0 | — | 0.0 | — |
| 100 | 0.0 | — | 0.0 | — |
| 150 | 1.0 | 0.9372 | 0.0 | — |
| 208 | 1.0 | 0.9818 | 0.0 | — |
| 300 | 1.0 | 0.9754 | 0.285 | 0.7296 |
| 400 | 1.0 | 0.9641 | 1.0 | 0.8516 |

**Table 5. Frozen design constants, their role, and the basis for each value (Section 3.2).** Every value is pinned literally by a unit test, so drift fails continuous integration. Pinning attests that a value was fixed before any result was read; it does not attest that the value is optimal. Only $M$ is swept (Section 4.9, Table 6).

| Constant | Value | Role | Basis for the value |
|---|---|---|---|
| Protocol seed | 20260721 | seeds the whole grid through one SHA-256 rule | arbitrary by design; the only requirement is that it is fixed in advance and never re-drawn |
| Split fractions | 0.40 / 0.20 / 0.40 | site-disjoint $S_{\text{train}}$ / $S_{\text{aux}}$ / $S_{\text{cal}}$ | set a priori, not optimized: fitting and certification take equal shares, and the auxiliary pool the smallest, since it only orders the walk and fits the correction |
| Budget ladder $\alpha$ | $\{0.05,\ 0.10\}$ | the two certified rungs | conventional error budgets; Section 4.5 shows which is reachable at which site count |
| Confidence $\delta$ | 0.05 | certificate error probability | convention |
| $\delta_{\text{conf}}$ / $\delta_{\text{bet}}$ | 0.025 / 0.025 | BBSE split of $\delta$ | an even split, not optimized; the union bound restores $1-\delta$ |
| Bonferroni width | 4 | box over $(c_0, c_1, \pi_s, q_t)$ | one interval per estimated parameter |
| Influence cap $M$ | 100 | $g_c = \min(n_c, M)$ | places the estimand near the site-equal end of the site/record continuum: $\approx 90\%$ of sites are capped. Swept in Table 6 |
| Threshold grid | 23 points, $0.55$ to $0.99$ step $0.02$ | candidate operating points | starts at 0.55 because $s(x) \in [0.5,1]$ and $\tau = 0.5$ answers everything; the fixed-sequence walk spends no $\delta$ on grid size, so resolution costs only compute |
| WSR bet cap | $0.9/(1-\alpha)$ | keeps every wealth factor $\ge 0.1 > 0$ | required for the supermartingale property (A.1(iii)); the audited v1 value |
| WSR variance floor | $10^{-8}$ | guards a zero variance estimate | numerical |
| WSR $(\hat\mu_0, \hat\sigma^2_0)$ | $(0.5,\ 0.25)$ | running-moment initialization | the maximum-variance start for a $[0,1]$ variable; being data-independent is what keeps $\lambda_t$ predictable |
| Min calibration clusters | 50 record-carrying | hard gate | below this the information floor exceeds any attainable margin; Section 4.5 keeps this gate distinct from the floor |
| Min target pool | 10 records | hard gate | no answered-set rate is meaningful below it; the gate declines rather than emit one |
| BBSE gap floor $(c_1 - c_0)$ | 0.10 | decline threshold | the confusion inversion divides by this gap; below 0.10 it is ill-conditioned |
| BBSE min target sites | 10 | decline threshold for the $q$ cluster bootstrap | measured $\rho$-interval miss up to 46% at $K = 2$ against a nominal 2.5%; pools declaring 2–9 sites decline |
| BBSE resamples | 2,000 valid within 4,000 attempts | bootstrap discipline | 2,000 keeps Monte-Carlo error small at the 2.5% quantile; the attempt cap forces a degenerate pool to decline rather than yield a quantile over a silently reduced count |
| Report bootstrap | 500 resamples | estimated-tier interval | estimated tier only; no bearing on the certificate |
| $\pi_t$ clip | $10^{-4}$ | keeps the implied prevalence inside $(0,1)$ | numerical; can only widen $[\rho_{\text{lo}}, \rho_{\text{hi}}]$ |
| Standardization tolerance | $10^{-9}$ relative | guards a near-constant feature column | numerical |
| Head $C$ / max iterations | 1.0 / 2,000 | L2 logistic head | library default regularization; the iteration cap is set for convergence, not selected |

**Table 6. Influence-cap sensitivity (E1 exchangeable arm, $s_u = 0.5$, baseline mode, $R = 200$; Section 4.9).** Each row re-derives the walk order and the calibration walk at that $M$ and rescores against $R_M$ *at that same $M$*, so each row certifies its own estimand. Coverage and $R_M$ are measured on the fresh 200-site evaluation pool. "—" where nothing certifies. The frozen value is $M = 100$.

| $M$ | Records above cap | $\alpha=0.10$ certify | Mean $\tau^*$ | Mean coverage | Mean $R_M(M)$ | $R_M$-exceed | $\alpha=0.05$ certify |
|---|---|---|---|---|---|---|---|
| 25 | 0.964 | 1.00 | 0.552 | 0.9843 | 0.0574 | 0.00 | 0.0 |
| 50 | 0.929 | 1.00 | 0.552 | 0.9843 | 0.0574 | 0.00 | 0.0 |
| **100** | 0.861 | 1.00 | 0.554 | 0.9838 | 0.0572 | 0.00 | 0.0 |
| 200 | 0.743 | 1.00 | 0.566 | 0.9799 | 0.0558 | 0.00 | 0.0 |
| 500 | 0.499 | 1.00 | 0.688 | 0.9388 | 0.0421 | 0.00 | 0.0 |
| 1000 | 0.289 | 0.03 | 0.827 | 0.8714 | 0.0270 | 0.00 | 0.0 |
| 5000 | 0.000 | 0.00 | — | — | — | — | 0.0 |

**Table 7. Answered- and declined-set operating characteristics at $\alpha = 0.10$ (Sections 4.2, 4.7).** Hard labels are $\hat{y} = \mathbb{1}[\hat{p}(x) \ge 1/2]$ under the symmetric 0–1 loss of Section 3.1. E1 pools all 200 fresh evaluation pools of the $s_u = 0.5$ arm; E6 is the single 40-site deployment at $\tau^* = 0.55$. A constant always-negative predictor would err at 0.1039 on E1's pooled records and 0.0968 on E6's pool — above $\alpha = 0.10$ in the first case and just below it in the second, which is why the sensitivity and prevalence columns, not the error column, carry the evidence that the gate is doing work.

| Set | Records | TP / FP / FN / TN | Positive fraction | Error | Sensitivity | Specificity | PPV | NPV |
|---|---|---|---|---|---|---|---|---|
| E1 answered (pooled, $R = 200$) | 27,972,430 | 1,487,807 / 366,089 / 1,218,592 / 24,899,942 | 0.0968 | 0.0567 | 0.550 | 0.986 | 0.803 | 0.953 |
| E1 declined (pooled, $R = 200$) | 517,230 | 128,494 / 109,406 / 125,909 / 153,421 | 0.4919 | 0.4550 | 0.505 | 0.584 | 0.540 | 0.549 |
| E6 answered (single draw) | 25,314 | 1,253 / 343 / 1,063 / 22,655 | 0.0915 | 0.0555 | 0.541 | 0.985 | 0.785 | 0.955 |
| E6 declined (single draw) | 368 | 82 / 74 / 88 / 124 | 0.4620 | 0.4402 | 0.482 | 0.626 | 0.526 | 0.585 |

# References

(Generated from references.bib by pandoc --citeproc at conversion time.)
